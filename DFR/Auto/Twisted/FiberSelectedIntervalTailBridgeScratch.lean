/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory Filter Set
open Auto.HardyLittlewoodMaximal
open scoped BigOperators ENNReal

noncomputable section

/-! ## Selected dyadic intervals as tail-test data

The canonical cutoff-duality theorem is stated for arbitrary midpoint/radius
interval data.  The source stopping construction instead indexes literal
half-open dyadic intervals.  These small adapters make the passage exact on
each fixed transverse fiber.
-/

/-- The midpoint of a literal source fiber dyadic interval. -/
def scratch_fiberDyadicIntervalCenter (I : FiberDyadicInterval) : ℝ :=
  ((I.2 : ℝ) + 1 / 2) * fiberDyadicIntervalLength I

/-- The half-length of a literal source fiber dyadic interval. -/
def scratch_fiberDyadicIntervalRadius (I : FiberDyadicInterval) : ℝ :=
  fiberDyadicIntervalLength I / 2

theorem scratch_fiberDyadicIntervalRadius_pos (I : FiberDyadicInterval) :
    0 < scratch_fiberDyadicIntervalRadius I := by
  unfold scratch_fiberDyadicIntervalRadius
  exact div_pos (fiberDyadicIntervalLength_pos I) (by norm_num)

theorem scratch_two_mul_fiberDyadicIntervalRadius (I : FiberDyadicInterval) :
    2 * scratch_fiberDyadicIntervalRadius I = fiberDyadicIntervalLength I := by
  unfold scratch_fiberDyadicIntervalRadius
  ring

theorem scratch_fiberDyadicInterval_center_sub_radius (I : FiberDyadicInterval) :
    scratch_fiberDyadicIntervalCenter I - scratch_fiberDyadicIntervalRadius I =
      (I.2 : ℝ) * fiberDyadicIntervalLength I := by
  unfold scratch_fiberDyadicIntervalCenter scratch_fiberDyadicIntervalRadius
  ring

theorem scratch_fiberDyadicInterval_center_add_radius (I : FiberDyadicInterval) :
    scratch_fiberDyadicIntervalCenter I + scratch_fiberDyadicIntervalRadius I =
      ((I.2 + 1 : ℤ) : ℝ) * fiberDyadicIntervalLength I := by
  unfold scratch_fiberDyadicIntervalCenter scratch_fiberDyadicIntervalRadius
  push_cast
  ring

/-- The open midpoint/radius interval is contained in the literal half-open
dyadic cell.  This is enough to transport pairwise disjointness. -/
theorem scratch_centeredOpen_subset_fiberDyadicInterval (I : FiberDyadicInterval) :
    Ioo (scratch_fiberDyadicIntervalCenter I - scratch_fiberDyadicIntervalRadius I)
      (scratch_fiberDyadicIntervalCenter I + scratch_fiberDyadicIntervalRadius I) ⊆
      fiberDyadicInterval I := by
  intro y hy
  rw [scratch_fiberDyadicInterval_center_sub_radius,
    scratch_fiberDyadicInterval_center_add_radius] at hy
  unfold fiberDyadicInterval
  exact ⟨hy.1.le, hy.2⟩

/-- A finite subfamily selected on one fixed fiber is pairwise disjoint in
the open midpoint/radius form used by the interval-tail theorem. -/
theorem scratch_pairwiseDisjoint_selectedFiber_centeredOpenIntervals
    {Z : Type*} [MeasurableSpace Z] (Zf : Set Z)
    (A : FiberDyadicInterval → Z → ℝ) (H : ℝ) (z : Z)
    (T : Finset FiberDyadicInterval)
    (hselected : ∀ I ∈ T, z ∈ fiberDyadicSelectionSet Zf A H I) :
    (↑T : Set FiberDyadicInterval).PairwiseDisjoint
      (fun I ↦ Ioo
        (scratch_fiberDyadicIntervalCenter I - scratch_fiberDyadicIntervalRadius I)
        (scratch_fiberDyadicIntervalCenter I + scratch_fiberDyadicIntervalRadius I)) := by
  intro I hI J hJ hne
  exact (pairwiseDisjoint_fiberDyadicSelectedIntervals Zf A H z
    (hselected I hI) (hselected J hJ) hne).mono
      (scratch_centeredOpen_subset_fiberDyadicInterval I)
      (scratch_centeredOpen_subset_fiberDyadicInterval J)

/-- The literal finite selected-fiber tail test.  It is the canonical
finite pairwise-disjoint cutoff/Hölder bound, with its midpoint/radius data
supplied directly by the source dyadic selection. -/
theorem scratch_selectedFiber_finset_intervalTail_holder_cutoff_le
    {Z : Type*} [MeasurableSpace Z] (Zf : Set Z)
    (A : FiberDyadicInterval → Z → ℝ) (H : ℝ) (z : Z)
    (T : Finset FiberDyadicInterval)
    (hselected : ∀ I ∈ T, z ∈ fiberDyadicSelectionSet Zf A H I)
    {p q : ℝ} (hpq : q.HolderConjugate p) (hq : 1 < q)
    (g : FiberDyadicLine → ℂ) (hg : Measurable g)
    (a : ℝ) (ha : 0 ≤ a) (hga : ∀ x, ‖g x‖ ≤ a)
    (hgp : MemLp g (ENNReal.ofReal q) volume) :
    (∑ I ∈ T, ∫ x : FiberDyadicLine,
      ‖g x‖ * intervalTail (scratch_fiberDyadicIntervalCenter I)
        (2 * scratch_fiberDyadicIntervalRadius I)
          (realToFiberDyadicLine.symm x)) ≤
      64 * (volume (Metric.ball (0 : FiberDyadicLine) 1)).toReal *
        (∫ u in ⋃ I ∈ T, Ioo
          (scratch_fiberDyadicIntervalCenter I - scratch_fiberDyadicIntervalRadius I)
          (scratch_fiberDyadicIntervalCenter I + scratch_fiberDyadicIntervalRadius I),
          (dyadicBallMaximal 1 g (realToFiberDyadicLine u)) ^ q) ^ (1 / q) *
        (volume (⋃ I ∈ T, Ioo
          (scratch_fiberDyadicIntervalCenter I - scratch_fiberDyadicIntervalRadius I)
          (scratch_fiberDyadicIntervalCenter I + scratch_fiberDyadicIntervalRadius I))).toReal ^
          (1 / p) := by
  apply finset_pairwiseDisjoint_intervalTail_holder_cutoff_le
    T scratch_fiberDyadicIntervalCenter scratch_fiberDyadicIntervalRadius
  · intro I hI
    exact scratch_fiberDyadicIntervalRadius_pos I
  · exact scratch_pairwiseDisjoint_selectedFiber_centeredOpenIntervals
      Zf A H z T hselected
  · exact hpq
  · exact hq
  · exact hg
  · exact ha
  · exact hga
  · exact hgp

end
end Twisted
end Auto
