/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory Filter TopologicalSpace
open scoped BigOperators ENNReal RealInnerProductSpace

noncomputable section

/-! ## Dominant-fiber envelope algebra -/

theorem scratch_dominant_zero_exponent
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1) (m : ℤ) :
    (m : ℝ) + q 0 / q 1 * (m : ℝ) + q 0 / q 2 * (m : ℝ) +
      q 0 / q 3 * (m : ℝ) = q 0 * (m : ℝ) := by
  have hsum' : (q 0)⁻¹ + (q 1)⁻¹ + (q 2)⁻¹ + (q 3)⁻¹ = 1 := by
    simpa only [Fin.sum_univ_four] using hsum
  field_simp [ne_of_gt (hq 0), ne_of_gt (hq 1), ne_of_gt (hq 2),
    ne_of_gt (hq 3)] at hsum' ⊢
  ring_nf at hsum' ⊢
  linear_combination (m : ℝ) * hsum'

theorem scratch_dominant_exponent_erase
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1)
    (j : Fin 4) (m : ℤ) :
    (m : ℝ) + (∑ k ∈ Finset.univ.erase j,
      q j / q k * (m : ℝ)) = q j * (m : ℝ) := by
  have hfull : (∑ k : Fin 4, q j / q k) = q j := by
    calc
      (∑ k : Fin 4, q j / q k) = ∑ k : Fin 4, q j * (q k)⁻¹ := by
        apply Finset.sum_congr rfl
        intro k hk
        rw [div_eq_mul_inv]
      _ = q j * (∑ k : Fin 4, (q k)⁻¹) := by
        rw [Finset.mul_sum]
      _ = q j * 1 := by rw [hsum]
      _ = q j := by ring
  have herase : 1 + (∑ k ∈ Finset.univ.erase j, q j / q k) = q j := by
    calc
      1 + (∑ k ∈ Finset.univ.erase j, q j / q k) =
          q j / q j + (∑ k ∈ Finset.univ.erase j, q j / q k) := by
            rw [div_self (ne_of_gt (hq j))]
      _ = ∑ k : Fin 4, q j / q k :=
        Finset.add_sum_erase Finset.univ (fun k ↦ q j / q k) (Finset.mem_univ j)
      _ = q j := hfull
  calc
    (m : ℝ) + (∑ k ∈ Finset.univ.erase j,
        q j / q k * (m : ℝ)) =
        1 * (m : ℝ) +
          (∑ k ∈ Finset.univ.erase j, q j / q k) * (m : ℝ) := by
            rw [one_mul, Finset.sum_mul]
    _ = (1 + ∑ k ∈ Finset.univ.erase j, q j / q k) * (m : ℝ) := by ring
    _ = q j * (m : ℝ) := by rw [herase]

theorem scratch_envelope_zero_case0
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1) (m : ℤ) :
    (2 : ℝ) ^ m * (2 * 2 ^ (q 0 / q 1 * (m : ℝ))) *
      (2 * 2 ^ (q 0 / q 2 * (m : ℝ))) *
      (2 * 2 ^ (q 0 / q 3 * (m : ℝ))) =
        8 * (2 : ℝ) ^ (q 0 * (m : ℝ)) := by
  have hcoeff := scratch_dominant_zero_exponent q hq hsum m
  have htwo : 0 < (2 : ℝ) := by norm_num
  have hpow : (2 : ℝ) ^ m * 2 ^ (q 0 / q 1 * (m : ℝ)) *
      2 ^ (q 0 / q 2 * (m : ℝ)) * 2 ^ (q 0 / q 3 * (m : ℝ)) =
        2 ^ (q 0 * (m : ℝ)) := by
    rw [← Real.rpow_intCast, ← Real.rpow_add htwo,
      ← Real.rpow_add htwo, ← Real.rpow_add htwo, hcoeff]
  calc
    (2 : ℝ) ^ m * (2 * 2 ^ (q 0 / q 1 * (m : ℝ))) *
        (2 * 2 ^ (q 0 / q 2 * (m : ℝ))) *
        (2 * 2 ^ (q 0 / q 3 * (m : ℝ))) =
        8 * ((2 : ℝ) ^ m * 2 ^ (q 0 / q 1 * (m : ℝ)) *
          2 ^ (q 0 / q 2 * (m : ℝ)) *
          2 ^ (q 0 / q 3 * (m : ℝ))) := by ring
    _ = 8 * (2 : ℝ) ^ (q 0 * (m : ℝ)) := by rw [hpow]

theorem scratch_envelope_zero_case1
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1) (m : ℤ) :
    (2 * 2 ^ (q 1 / q 0 * (m : ℝ))) * (2 : ℝ) ^ m *
      (2 * 2 ^ (q 1 / q 2 * (m : ℝ))) *
      (2 * 2 ^ (q 1 / q 3 * (m : ℝ))) =
        8 * (2 : ℝ) ^ (q 1 * (m : ℝ)) := by
  have hcoeff : q 1 / q 0 * (m : ℝ) + (m : ℝ) +
      q 1 / q 2 * (m : ℝ) + q 1 / q 3 * (m : ℝ) =
        q 1 * (m : ℝ) := by
    have hsum' : (q 0)⁻¹ + (q 1)⁻¹ + (q 2)⁻¹ + (q 3)⁻¹ = 1 := by
      simpa only [Fin.sum_univ_four] using hsum
    field_simp [ne_of_gt (hq 0), ne_of_gt (hq 1), ne_of_gt (hq 2),
      ne_of_gt (hq 3)] at hsum' ⊢
    ring_nf at hsum' ⊢
    linear_combination (m : ℝ) * hsum'
  have htwo : 0 < (2 : ℝ) := by norm_num
  have hpow : 2 ^ (q 1 / q 0 * (m : ℝ)) * (2 : ℝ) ^ m *
      2 ^ (q 1 / q 2 * (m : ℝ)) * 2 ^ (q 1 / q 3 * (m : ℝ)) =
        2 ^ (q 1 * (m : ℝ)) := by
    rw [← Real.rpow_intCast, ← Real.rpow_add htwo,
      ← Real.rpow_add htwo, ← Real.rpow_add htwo, hcoeff]
  calc
    (2 * 2 ^ (q 1 / q 0 * (m : ℝ))) * (2 : ℝ) ^ m *
        (2 * 2 ^ (q 1 / q 2 * (m : ℝ))) *
        (2 * 2 ^ (q 1 / q 3 * (m : ℝ))) =
        8 * (2 ^ (q 1 / q 0 * (m : ℝ)) * (2 : ℝ) ^ m *
          2 ^ (q 1 / q 2 * (m : ℝ)) * 2 ^ (q 1 / q 3 * (m : ℝ))) := by ring
    _ = 8 * (2 : ℝ) ^ (q 1 * (m : ℝ)) := by rw [hpow]

theorem scratch_envelope_zero_case2
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1) (m : ℤ) :
    (2 * 2 ^ (q 2 / q 0 * (m : ℝ))) *
      (2 * 2 ^ (q 2 / q 1 * (m : ℝ))) * (2 : ℝ) ^ m *
      (2 * 2 ^ (q 2 / q 3 * (m : ℝ))) =
        8 * (2 : ℝ) ^ (q 2 * (m : ℝ)) := by
  have hcoeff : q 2 / q 0 * (m : ℝ) + q 2 / q 1 * (m : ℝ) +
      (m : ℝ) + q 2 / q 3 * (m : ℝ) = q 2 * (m : ℝ) := by
    have hsum' : (q 0)⁻¹ + (q 1)⁻¹ + (q 2)⁻¹ + (q 3)⁻¹ = 1 := by
      simpa only [Fin.sum_univ_four] using hsum
    field_simp [ne_of_gt (hq 0), ne_of_gt (hq 1), ne_of_gt (hq 2),
      ne_of_gt (hq 3)] at hsum' ⊢
    ring_nf at hsum' ⊢
    linear_combination (m : ℝ) * hsum'
  have htwo : 0 < (2 : ℝ) := by norm_num
  have hpow : 2 ^ (q 2 / q 0 * (m : ℝ)) *
      2 ^ (q 2 / q 1 * (m : ℝ)) * (2 : ℝ) ^ m *
      2 ^ (q 2 / q 3 * (m : ℝ)) = 2 ^ (q 2 * (m : ℝ)) := by
    rw [← Real.rpow_intCast, ← Real.rpow_add htwo,
      ← Real.rpow_add htwo, ← Real.rpow_add htwo, hcoeff]
  calc
    (2 * 2 ^ (q 2 / q 0 * (m : ℝ))) *
        (2 * 2 ^ (q 2 / q 1 * (m : ℝ))) * (2 : ℝ) ^ m *
        (2 * 2 ^ (q 2 / q 3 * (m : ℝ))) =
        8 * (2 ^ (q 2 / q 0 * (m : ℝ)) *
          2 ^ (q 2 / q 1 * (m : ℝ)) * (2 : ℝ) ^ m *
          2 ^ (q 2 / q 3 * (m : ℝ))) := by ring
    _ = 8 * (2 : ℝ) ^ (q 2 * (m : ℝ)) := by rw [hpow]

theorem scratch_envelope_zero_case3
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1) (m : ℤ) :
    (2 * 2 ^ (q 3 / q 0 * (m : ℝ))) *
      (2 * 2 ^ (q 3 / q 1 * (m : ℝ))) *
      (2 * 2 ^ (q 3 / q 2 * (m : ℝ))) * (2 : ℝ) ^ m =
        8 * (2 : ℝ) ^ (q 3 * (m : ℝ)) := by
  have hcoeff : q 3 / q 0 * (m : ℝ) + q 3 / q 1 * (m : ℝ) +
      q 3 / q 2 * (m : ℝ) + (m : ℝ) = q 3 * (m : ℝ) := by
    have hsum' : (q 0)⁻¹ + (q 1)⁻¹ + (q 2)⁻¹ + (q 3)⁻¹ = 1 := by
      simpa only [Fin.sum_univ_four] using hsum
    field_simp [ne_of_gt (hq 0), ne_of_gt (hq 1), ne_of_gt (hq 2),
      ne_of_gt (hq 3)] at hsum' ⊢
    ring_nf at hsum' ⊢
    linear_combination (m : ℝ) * hsum'
  have htwo : 0 < (2 : ℝ) := by norm_num
  have hpow : 2 ^ (q 3 / q 0 * (m : ℝ)) *
      2 ^ (q 3 / q 1 * (m : ℝ)) * 2 ^ (q 3 / q 2 * (m : ℝ)) *
      (2 : ℝ) ^ m = 2 ^ (q 3 * (m : ℝ)) := by
    rw [← Real.rpow_intCast, ← Real.rpow_add htwo,
      ← Real.rpow_add htwo, ← Real.rpow_add htwo, hcoeff]
  calc
    (2 * 2 ^ (q 3 / q 0 * (m : ℝ))) *
        (2 * 2 ^ (q 3 / q 1 * (m : ℝ))) *
        (2 * 2 ^ (q 3 / q 2 * (m : ℝ))) * (2 : ℝ) ^ m =
        8 * (2 ^ (q 3 / q 0 * (m : ℝ)) *
          2 ^ (q 3 / q 1 * (m : ℝ)) *
          2 ^ (q 3 / q 2 * (m : ℝ)) * (2 : ℝ) ^ m) := by ring
    _ = 8 * (2 : ℝ) ^ (q 3 * (m : ℝ)) := by rw [hpow]

/-- With the normalization shifts set to zero, the finite dominant-fiber
envelope is a fixed geometric factor times the `q_j`-power dyadic weight. -/
theorem scratch_sourceStoppingDominantFiberEnvelope_zero_eq
    (q : Fin 4 → ℝ) (hq : ∀ j, 0 < q j)
    (hsum : ∑ j : Fin 4, (q j)⁻¹ = 1)
    (j : Fin 4) (m : ℤ) :
    sourceStoppingDominantFiberEnvelope q (fun _ ↦ 0) j m =
      8 * (2 : ℝ) ^ (q j * (m : ℝ)) := by
  fin_cases j
  · simpa [sourceStoppingDominantFiberEnvelope, Fin.prod_univ_four] using
      scratch_envelope_zero_case0 q hq hsum m
  · simpa [sourceStoppingDominantFiberEnvelope, Fin.prod_univ_four] using
      scratch_envelope_zero_case1 q hq hsum m
  · simpa [sourceStoppingDominantFiberEnvelope, Fin.prod_univ_four] using
      scratch_envelope_zero_case2 q hq hsum m
  · simpa [sourceStoppingDominantFiberEnvelope, Fin.prod_univ_four] using
      scratch_envelope_zero_case3 q hq hsum m

end
end Twisted
end Auto
