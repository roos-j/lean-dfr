/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted
import LeanSpherical.Auto.Spherical.Auxiliary
import LeanSpherical.Auto.Spherical.MSSBase

/-! ### Audit of the proposed sequential strong/weak interpolation route

For a frozen trilinear slice, write `a > 0` for the passive reciprocal
sum and `x > 0` for the active reciprocal.  The unary map has input
exponent `x⁻¹` and output exponent `(a + x)⁻¹`, so all of its endpoints
have output/input ratio below one.  This file records the directly callable
generic engines.  The two-pair source/output engine is fully general only
after the caller supplies two weighted split-tail inequalities; no supplied
tail theorem in the available interface covers this ratio direction.
-/

namespace Auto.Twisted

open MeasureTheory

noncomputable section

#check Auto.Spherical.MSSBase.sourceOutput_two_pair_marcinkiewicz_moment_of_split_weak_endpoints
#check Auto.Spherical.MSSBase.sourceOutput_two_pair_marcinkiewicz_moment_of_strong_endpoints_and_split_tails
#check Auto.Spherical.MSSBase.sourceOutput_two_pair_eLpNorm_le_of_scaled_strong_endpoints_and_split_tails
#check Auto.Spherical.MSSBase.sourceOutput_marcinkiewicz_weak_q_top_on_additive_split
#check Auto.Spherical.Auxiliary.marcinkiewicz_weak_one_two_on_additive_split
#check Auto.Spherical.Auxiliary.marcinkiewicz_weak_q_top_on_additive_split

/-- The source/output ratio of every frozen trilinear slice is strictly
below one. -/
theorem scratch_frozenSlice_sourceOutputRatio_lt_one
    {a x : ℝ} (ha : 0 < a) (hx : 0 < x) :
    ((a + x)⁻¹) / x⁻¹ < 1 := by
  have hax : 0 < a + x := add_pos ha hx
  have hx0 : x ≠ 0 := ne_of_gt hx
  have hax0 : a + x ≠ 0 := ne_of_gt hax
  field_simp
  linarith

/-- Thus the hypothesis `1 ≤ qᵢ / pᵢ` used by the existing off-diagonal
smooth-tail producer cannot hold at either endpoint of such a slice. -/
theorem scratch_frozenSlice_not_offDiagonalTailRatio
    {a x : ℝ} (ha : 0 < a) (hx : 0 < x) :
    ¬ (1 : ℝ) ≤ ((a + x)⁻¹) / x⁻¹ := by
  exact not_le_of_gt (scratch_frozenSlice_sourceOutputRatio_lt_one ha hx)

end

end Auto.Twisted
