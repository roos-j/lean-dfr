/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto

open MeasureTheory Filter TopologicalSpace
open scoped BigOperators ENNReal NNReal FourierTransform RealInnerProductSpace Topology

noncomputable section

namespace Twisted

/-- A convergent family of finite expanded forest forms transfers its uniform
bound to the corresponding finite scale truncation.  The outer index exhausts
the spatial/scale forest while the scale cutoff remains fixed. -/
theorem scratch_modelScaleTruncation_bound_of_tendsto_expandedModelLocalForm
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (F : ModelSchwartzInput)
    (f : ModelRealInput) (B : ℝ) (N : ℕ)
    (hlocalization : Tendsto
      (fun M ↦ expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f)
      atTop (𝓝 (ModelScaleTruncation α u c F N)))
    (hforest : ∀ M : ℕ,
      |expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f| ≤ B) :
    |ModelScaleTruncation α u c F N| ≤ B := by
  have hlimabs : Tendsto
      (fun M ↦ |expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f|)
      atTop (𝓝 |ModelScaleTruncation α u c F N|) := by
    exact (continuous_abs.tendsto _).comp hlocalization
  apply le_of_tendsto hlimabs
  exact Filter.Eventually.of_forall hforest

/-- The two-index finite-forest consumer.  For each fixed dyadic scale band,
first exhaust the finite expanded forest; then let the scale band exhaust the
positive half-line through the profile bound. -/
theorem scratch_modelFullForm_bound_of_profile_and_twoIndex_truncationForest_bound
    {a C : ℝ} (ha : 0 < a)
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ)
    (F : ModelSchwartzInput) (f : ModelRealInput) (B : ℝ)
    (hc : ∀ t : ℝ, |c t| ≤ 1)
    (hmeas : AEStronglyMeasurable
      (fun t : ℝ ↦ c t * ModelSpatialProfile α u F t)
      (((volume : Measure ℝ).withDensity cubeScaleDensity).restrict
        (Set.Ioi (0 : ℝ))))
    (hprofile : ∀ t : ℝ, 0 < t →
      |ModelSpatialProfile α u F t| ≤ C * min (t ^ a) (t ^ (-a)))
    (hlocalization : ∀ N : ℕ, Tendsto
      (fun M ↦ expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f)
      atTop (𝓝 (ModelScaleTruncation α u c F N)))
    (hforest : ∀ N M : ℕ,
      |expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f| ≤ B) :
    |ModelFullForm α u c F| ≤ B := by
  apply modelFullForm_bound_of_profile_and_uniform_truncation_bound
    ha α u c F B hc hmeas hprofile
  intro N
  apply scratch_modelScaleTruncation_bound_of_tendsto_expandedModelLocalForm
    α u c F f B N
  · exact hlocalization N
  · exact hforest N

/-- A finite-forest estimate uniform over all unit-bounded coefficients gives
the two-index cutoff hypothesis automatically. -/
theorem scratch_modelFullForm_bound_of_profile_and_twoIndex_uniformForest_bound
    {a C : ℝ} (ha : 0 < a)
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ)
    (F : ModelSchwartzInput) (f : ModelRealInput) (B : ℝ)
    (hc : ∀ t : ℝ, |c t| ≤ 1)
    (hmeas : AEStronglyMeasurable
      (fun t : ℝ ↦ c t * ModelSpatialProfile α u F t)
      (((volume : Measure ℝ).withDensity cubeScaleDensity).restrict
        (Set.Ioi (0 : ℝ))))
    (hprofile : ∀ t : ℝ, 0 < t →
      |ModelSpatialProfile α u F t| ≤ C * min (t ^ a) (t ^ (-a)))
    (hlocalization : ∀ N : ℕ, Tendsto
      (fun M ↦ expandedModelLocalForm α (truncationCollection α M) u
        (RestrictScaleCoefficient c (truncationScaleSet N)) f)
      atTop (𝓝 (ModelScaleTruncation α u c F N)))
    (hforest : ∀ (d : ℝ → ℝ), (∀ t : ℝ, |d t| ≤ 1) → ∀ M : ℕ,
      |expandedModelLocalForm α (truncationCollection α M) u d f| ≤ B) :
    |ModelFullForm α u c F| ≤ B := by
  apply scratch_modelFullForm_bound_of_profile_and_twoIndex_truncationForest_bound
    ha α u c F f B hc hmeas hprofile hlocalization
  intro N M
  exact hforest (RestrictScaleCoefficient c (truncationScaleSet N))
    (abs_restrictScaleCoefficient_le_one c (truncationScaleSet N) hc) M

end Twisted

end

end Auto
