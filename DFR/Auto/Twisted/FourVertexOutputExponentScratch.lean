/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

noncomputable section

/-! ## Output-exponent geometry of the Section 8 simplex

The base vertex has output reciprocal `1 - b₀`, while each edge has output
exponent one and the target has output reciprocal `1 - β₀`.  This elementary
calculation is what places the target strictly between the weak-one and base
strong output exponents in the finite two-sided layer argument.
-/

/-- The target output exponent lies strictly between the weak-one endpoint
and the stronger base exponent of an `ExponentSimplex`. -/
theorem scratch_exponentSimplex_output_exponents
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3) :
    1 < (1 - β0)⁻¹ ∧ (1 - β0)⁻¹ < (1 - b.b0)⁻¹ := by
  have hβ0lt : β0 < 1 := by
    linarith [b.b0_gt, b.b0_lt_quarter]
  have htarget_pos : 0 < 1 - β0 := by linarith
  have htarget_lt_one : 1 - β0 < 1 := by linarith [b.beta0_pos]
  have hbase_pos : 0 < 1 - b.b0 := by
    linarith [b.b0_lt_quarter]
  have hden_lt : 1 - b.b0 < 1 - β0 := by
    linarith [b.b0_gt]
  constructor
  · have h : 1 < 1 / (1 - β0) := by
      apply (lt_div_iff₀ htarget_pos).mpr
      simpa only [one_mul] using htarget_lt_one
    simpa only [one_div] using h
  · simpa only [one_div] using one_div_lt_one_div_of_lt hbase_pos hden_lt

end
end Twisted
end Auto
