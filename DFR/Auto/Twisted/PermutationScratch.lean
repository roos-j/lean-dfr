/- Copyright (c) 2026 Polona Durcik. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto.Twisted

open MeasureTheory Filter TopologicalSpace
open scoped BigOperators ENNReal RealInnerProductSpace Topology

noncomputable section

/-- Relabel the three Euclidean coordinates by a prescribed permutation.
The convention is the one in the blueprint: coordinate `i` of the image is
coordinate `σ i` of the input. -/
noncomputable def scratch_coordinatePermutation
    (σ : Equiv.Perm (Fin 3)) : E3 ≃ₗᵢ[ℝ] E3 :=
  LinearIsometryEquiv.piLpCongrLeft 2 ℝ ℝ σ.symm

@[simp] theorem scratch_coordinatePermutation_apply
    (σ : Equiv.Perm (Fin 3)) (x : E3) (i : Fin 3) :
    scratch_coordinatePermutation σ x i = x (σ i) := by
  rfl

/-- The coordinate relabeling is Lebesgue-measure preserving. -/
theorem scratch_coordinatePermutation_measurePreserving
    (σ : Equiv.Perm (Fin 3)) :
    MeasurePreserving (scratch_coordinatePermutation σ) volume volume := by
  exact LinearIsometryEquiv.measurePreserving _

/-- Relabel the anisotropy in the same coordinate convention. -/
def scratch_permutedAnisotropy (α : Anisotropy)
    (σ : Equiv.Perm (Fin 3)) : Anisotropy where
  weight i := α.weight (σ i)
  weight_pos i := α.weight_pos (σ i)

@[simp] theorem scratch_permutedAnisotropy_weight
    (α : Anisotropy) (σ : Equiv.Perm (Fin 3)) (i : Fin 3) :
    (scratch_permutedAnisotropy α σ).weight i = α.weight (σ i) := rfl

/-- Coordinate relabeling intertwines the two anisotropic dilations. -/
theorem scratch_coordinatePermutation_dilate
    (α : Anisotropy) (σ : Equiv.Perm (Fin 3)) (t : ℝ) (x : E3) :
    scratch_coordinatePermutation σ (α.dilate t x) =
      (scratch_permutedAnisotropy α σ).dilate t
        (scratch_coordinatePermutation σ x) := by
  ext i
  simp [Anisotropy.dilate]

end

end Auto.Twisted
