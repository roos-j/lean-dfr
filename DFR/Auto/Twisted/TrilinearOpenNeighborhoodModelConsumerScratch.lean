/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.TrilinearOpenNeighborhoodGeometryScratch

namespace Auto
namespace Twisted

open scoped BigOperators

noncomputable section

/-! ## Literal active-model consumer for finite restricted atoms

This is deliberately a consumer, rather than an assertion of the missing
Calderón--Zygmund endpoint.  It records the exact analytic facts needed to
plug a one-fiber restricted estimate into the finite open-neighbourhood
summation:

* a fixed dual Schwartz input turns the literal four-linear model form into
  a complex trilinear form in slots `1,2,3`; and
* normalized atoms satisfy a restricted form estimate at the strict
  reciprocal triple selected by one amplitude-sign quadrant.

The currently canonical strict initial-range theorem supplies only the base
vertex.  The missing one-fiber CZ theorem must establish the corresponding
edge/open-neighbourhood atom estimates and the displayed trilinear
representation.  No weak-to-pairing implication is hidden here.
-/

/-- A concrete complex trilinear representation of the literal active model
after fixing its zeroth (dual) Schwartz input.  The equation is the precise
finite-multilinearity bridge still needed from the literal scale integral. -/
def scratch_IsLiteralActiveModelFullFormTrilinearRepresentation
    (α : Anisotropy) (active : Fin 3) (u : E3) (c : ℝ → ℂ)
    (f₀ : SchwartzMap E3 ℂ)
    (B : (SchwartzMap E3 ℂ) →ₗ[ℂ]
      (SchwartzMap E3 ℂ) →ₗ[ℂ] (SchwartzMap E3 ℂ) →ₗ[ℂ] ℂ) : Prop :=
  ∀ f₁ f₂ f₃,
    B f₁ f₂ f₃ =
      LiteralActiveModelFullForm α active u c ![f₀, f₁, f₂, f₃]

/-- The exact finite atomwise restricted estimate consumed by one sign
quadrant of the literal active-model interpolation step.  The `m_j` are the
measures of normalized input atoms; the exponents `a_j` are reciprocal input
exponents chosen strictly on the sides prescribed by the dyadic signs. -/
def scratch_LiteralActiveModelRestrictedAtoms
    (α : Anisotropy) (active : Fin 3) (u : E3) (c : ℝ → ℂ)
    (f₀ : SchwartzMap E3 ℂ)
    (s₁ s₂ s₃ : Finset ℕ)
    (g₁ g₂ g₃ : ℕ → SchwartzMap E3 ℂ)
    (m₁ m₂ m₃ : ℕ → ℝ) (a₁ a₂ a₃ C : ℝ) : Prop :=
  ∀ i ∈ s₁, ∀ j ∈ s₂, ∀ k ∈ s₃,
    ‖LiteralActiveModelFullForm α active u c ![f₀, g₁ i, g₂ j, g₃ k]‖ ≤
      C * (m₁ i) ^ a₁ * (m₂ j) ^ a₂ * (m₃ k) ^ a₃

/-- The literal active model obeys the finite fixed-sign dyadic bound as
soon as its actual scale integral has been represented trilinearly and the
one-fiber analysis supplies the displayed restricted atom estimate.  This
is the direct source-facing consumer of the analytic endpoint package. -/
theorem scratch_LiteralActiveModelFullForm_fixedSign_dyadic_consumer
    (α : Anisotropy) (active : Fin 3) (u : E3) (c : ℝ → ℂ)
    (f₀ : SchwartzMap E3 ℂ)
    (B : (SchwartzMap E3 ℂ) →ₗ[ℂ]
      (SchwartzMap E3 ℂ) →ₗ[ℂ] (SchwartzMap E3 ℂ) →ₗ[ℂ] ℂ)
    (hB : scratch_IsLiteralActiveModelFullFormTrilinearRepresentation
      α active u c f₀ B)
    (s₁ s₂ s₃ : Finset ℕ)
    (g₁ g₂ g₃ : ℕ → SchwartzMap E3 ℂ)
    (z₁ z₂ z₃ : ℕ → ℂ)
    (m₁ m₂ m₃ : ℕ → ℝ)
    (positive₁ positive₂ positive₃ : Bool)
    (p₁ p₂ p₃ a₁ a₂ a₃ C : ℝ)
    (hp₁ : 0 < p₁) (hp₂ : 0 < p₂) (hp₃ : 0 < p₃)
    (ha₁ : 0 ≤ a₁) (ha₂ : 0 ≤ a₂) (ha₃ : 0 ≤ a₃)
    (hside₁ : if positive₁ then p₁⁻¹ < a₁ else a₁ < p₁⁻¹)
    (hside₂ : if positive₂ then p₂⁻¹ < a₂ else a₂ < p₂⁻¹)
    (hside₃ : if positive₃ then p₃⁻¹ < a₃ else a₃ < p₃⁻¹)
    (hC : 0 ≤ C)
    (hm₁ : ∀ i ∈ s₁, 0 ≤ m₁ i)
    (hm₂ : ∀ j ∈ s₂, 0 ≤ m₂ j)
    (hm₃ : ∀ k ∈ s₃, 0 ≤ m₃ k)
    (hlevel₁ : ∀ i ∈ s₁, ‖z₁ i‖ =
      (2 : ℝ) ^ scratch_fixedSignDyadicHeight positive₁ i)
    (hlevel₂ : ∀ j ∈ s₂, ‖z₂ j‖ =
      (2 : ℝ) ^ scratch_fixedSignDyadicHeight positive₂ j)
    (hlevel₃ : ∀ k ∈ s₃, ‖z₃ k‖ =
      (2 : ℝ) ^ scratch_fixedSignDyadicHeight positive₃ k)
    (hmass₁ : ∀ i ∈ s₁,
      (2 : ℝ) ^ (scratch_fixedSignDyadicHeight positive₁ i * p₁) * m₁ i ≤ 1)
    (hmass₂ : ∀ j ∈ s₂,
      (2 : ℝ) ^ (scratch_fixedSignDyadicHeight positive₂ j * p₂) * m₂ j ≤ 1)
    (hmass₃ : ∀ k ∈ s₃,
      (2 : ℝ) ^ (scratch_fixedSignDyadicHeight positive₃ k * p₃) * m₃ k ≤ 1)
    (hatom : scratch_LiteralActiveModelRestrictedAtoms
      α active u c f₀ s₁ s₂ s₃ g₁ g₂ g₃ m₁ m₂ m₃ a₁ a₂ a₃ C) :
    ‖LiteralActiveModelFullForm α active u c
      ![f₀, ∑ i ∈ s₁, z₁ i • g₁ i,
        ∑ j ∈ s₂, z₂ j • g₂ j, ∑ k ∈ s₃, z₃ k • g₃ k]‖ ≤
      C * scratch_fixedSignDyadicDecay positive₁ p₁ a₁ *
        scratch_fixedSignDyadicDecay positive₂ p₂ a₂ *
          scratch_fixedSignDyadicDecay positive₃ p₃ a₃ := by
  have hgeneric := scratch_norm_trilinear_finset_sum_le_of_fixedSign_dyadic_atoms
    B s₁ s₂ s₃ g₁ g₂ g₃ z₁ z₂ z₃ m₁ m₂ m₃
    positive₁ positive₂ positive₃ p₁ p₂ p₃ a₁ a₂ a₃ C
    hp₁ hp₂ hp₃ ha₁ ha₂ ha₃ hside₁ hside₂ hside₃ hC
    hm₁ hm₂ hm₃ hlevel₁ hlevel₂ hlevel₃ hmass₁ hmass₂ hmass₃
    (by
      intro i hi j hj k hk
      rw [hB]
      exact hatom i hi j hj k hk)
  rw [← hB]
  exact hgeneric

end
end Twisted
end Auto
