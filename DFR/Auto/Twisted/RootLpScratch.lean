/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory Filter TopologicalSpace
open scoped BigOperators ENNReal RealInnerProductSpace

noncomputable section

/-! ## Root conversion for stopping majorants -/

noncomputable def rootMajorant (s : ℝ) (G : E3 → ℂ) : E3 → ℝ :=
  fun x ↦ ‖G x‖ ^ s⁻¹

theorem rootMajorant_nonneg (s : ℝ) (G : E3 → ℂ) (x : E3) :
    0 ≤ rootMajorant s G x := by
  exact Real.rpow_nonneg (norm_nonneg _) _

theorem rootMajorant_pow_eq (s q : ℝ) (hs : s ≠ 0)
    (G : E3 → ℂ) (x : E3) :
    rootMajorant s G x ^ q = ‖G x‖ ^ (q / s) := by
  unfold rootMajorant
  rw [← Real.rpow_mul (norm_nonneg _)]
  congr 1
  field_simp

theorem integrable_rootMajorant_pow_of_memLp
    (s q : ℝ) (hs : 0 < s) (hq : 0 < q)
    (G : E3 → ℂ)
    (hG : MemLp G (ENNReal.ofReal (q / s)) volume) :
    Integrable (fun x : E3 ↦ rootMajorant s G x ^ q) := by
  have hratio : 0 < q / s := div_pos hq hs
  have hinter : Integrable (fun x : E3 ↦ ‖G x‖ ^ (q / s)) := by
    simpa only [ENNReal.toReal_ofReal hratio.le] using
      hG.integrable_norm_rpow
        (ENNReal.ofReal_ne_zero_iff.mpr hratio)
        ENNReal.ofReal_ne_top
  apply hinter.congr
  filter_upwards [] with x
  exact (rootMajorant_pow_eq s q hs.ne' G x).symm

end
end Twisted
end Auto
