/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

/-! ### Audit of the proposed sequential unary interpolation route

For a frozen trilinear slice, let `a` be the sum of the reciprocal
exponents of the two passive inputs and let `x` be the reciprocal exponent of
the active input.  Its input and output exponents are respectively `x⁻¹` and
`(a + x)⁻¹`.  The public off-diagonal smooth-split engine requires the
opposite inequality `1 ≤ q / p`; this file checks that it cannot hold here.

API evidence: `LeanSpherical/Auto/Spherical/FractalDilations/RSUpperBounds.lean`,
lines 31977--31991, declares
`exists_twoPair_interpolation_const` with endpoint hypotheses
`hr0 : 1 ≤ q0 / p0` and `hr1 : 1 ≤ q1 / p1`.  Its `.olean` is not built in
the pinned checkout, so the checked facts below isolate the exact arithmetic
counter-condition without introducing a source import that cannot elaborate.
-/

namespace Auto.Twisted

noncomputable section

/-- The input exponent of one frozen coordinate, written from its reciprocal
exponent. -/
def scratchFrozenSliceInputExponent (x : ℝ) : ℝ := x⁻¹

/-- The output exponent of the same frozen trilinear slice. -/
def scratchFrozenSliceOutputExponent (a x : ℝ) : ℝ := (a + x)⁻¹

/-- A frozen trilinear slice always has output exponent strictly below its
active input exponent when the passive reciprocal sum is positive. -/
theorem scratchFrozenSlice_outputExponent_div_inputExponent_lt_one
    {a x : ℝ} (ha : 0 < a) (hx : 0 < x) :
    scratchFrozenSliceOutputExponent a x /
        scratchFrozenSliceInputExponent x < 1 := by
  rw [scratchFrozenSliceOutputExponent, scratchFrozenSliceInputExponent]
  have hax : 0 < a + x := add_pos ha hx
  have hx0 : x ≠ 0 := ne_of_gt hx
  have hax0 : a + x ≠ 0 := ne_of_gt hax
  field_simp
  linarith

/-- Hence the ratio hypothesis in the available `RSUpperBounds` two-pair
smooth-split theorem is unavailable on every such slice. -/
theorem scratchFrozenSlice_not_RSUpperBounds_twoPair_ratio
    {a x : ℝ} (ha : 0 < a) (hx : 0 < x) :
    ¬ (1 : ℝ) ≤ scratchFrozenSliceOutputExponent a x /
        scratchFrozenSliceInputExponent x := by
  exact not_le_of_gt
    (scratchFrozenSlice_outputExponent_div_inputExponent_lt_one ha hx)

end

end Auto.Twisted
