/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Claude.
-/

import Auto.Twisted.Twisted

/-!
# Conventions and the main statement

Section 1 of `blueprints/task_3_twisted_blueprint.tex`.

The definitions of this section (`def:anisotropy`, `def:multiplier`) are
already available in `Auto.Twisted`; this file carries the section's labelled
theorems, stated in the blueprint's own wording.
-/

namespace Auto
namespace Twisted

open MeasureTheory
open scoped BigOperators ENNReal

noncomputable section

/-- **`lem:pairing`** (Well-defined multiplier pairing).

The function `P_f` is Schwartz, the defining integral for `Λ_m` is absolutely
convergent, and the stated distributional identity holds.

The Schwartz assertion is carried by the type of `twistedProduct`; this is the
absolute-convergence assertion. -/
theorem lem_pairing_integrable
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (f₀ f₁ f₂ f₃ : Schwartz3) :
    Integrable (fun ξ : E3 ↦
      m ξ * schwartzFourier (twistedProduct f₀ f₁ f₂ f₃) (-ξ)) :=
  multiplierForm_integrable α M m hm f₀ f₁ f₂ f₃

/-- **`lem:pairing`**, equation `eq:frequency_form`.

Writing `ζ = (ζ⁽¹⁾, ζ⁽²⁾, ζ⁽³⁾) ∈ E₉` and `η(ζ) = (ζ⁽¹⁾₁, ζ⁽²⁾₂, ζ⁽³⁾₃)`, one
has the absolutely convergent frequency representation
`Λ_m(f) = ∫_{E₉} m(-η(ζ)) f̂₀(-∑_{j=1}^3 ζ⁽ʲ⁾) ∏_{j=1}^3 f̂_j(ζ⁽ʲ⁾) dζ`. -/
theorem lem_pairing_eq_frequencyForm
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (f₀ f₁ f₂ f₃ : Schwartz3) :
    multiplierForm m f₀ f₁ f₂ f₃ = frequencyForm m f₀ f₁ f₂ f₃ :=
  multiplierForm_eq_frequencyForm α M m hm f₀ f₁ f₂ f₃

/-- **`thm:main`** (Anisotropic paraproduct).

Suppose `4 < p₀ < ∞`, `1 < p₁, p₂, p₃ < 4`, and `∑_{j=0}^3 p_j^{-1} = 1`.
There exists `C_{α,p} > 0` such that every multiplier in Definition
`def:multiplier` and every complex Schwartz tuple satisfy
`|Λ_m(f)| ≤ C_{α,p} M ∏_{j=0}^3 ‖f_j‖_{p_j}`.

The single hypothesis `hU` is the external interpolation theorem
`ext:interpolation`, which the blueprint quotes and does not prove. -/
theorem thm_main
    (α : Anisotropy) (p : Fin 4 → ℝ)
    (hU : FourVertexMarcinkiewiczUniform (volume : Measure E3))
    (hp0 : 4 < p 0)
    (hp1 : 1 < p 1) (hp1' : p 1 < 4)
    (hp2 : 1 < p 2) (hp2' : p 2 < 4)
    (hp3 : 1 < p 3) (hp3' : p 3 < 4)
    (hsum : ∑ j : Fin 4, (p j)⁻¹ = 1) :
    ∃ C : ℝ, 0 ≤ C ∧ ∀ (M : ℝ) (m : E3 → ℂ),
      Anisotropy.IsAnisotropicMultiplier α M m →
      ∀ F : ModelComplexSchwartzInput,
        ‖multiplierForm m (F 0) (F 1) (F 2) (F 3)‖
          ≤ C * M *
              ∏ j : Fin 4,
                lpNorm ((F j : E3 → ℂ)) (ENNReal.ofReal (p j)) volume :=
  anisotropicParaproduct_of_interpolation α p hU hp0 hp1 hp1' hp2 hp2' hp3 hp3'
    hsum

end

end Twisted
end Auto
