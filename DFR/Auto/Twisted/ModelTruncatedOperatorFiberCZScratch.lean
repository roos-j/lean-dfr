/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.FiberCZWeakAssemblyScratch

namespace Auto
namespace Twisted

open MeasureTheory
open scoped ENNReal

noncomputable section

/-! ## Weak-one consumer for the actual truncated model operator

This is the source-facing last line of the one-fiber CZ argument.  The
analytic construction must provide its three hypotheses: the selected
exceptional-set budget, the good-part threshold estimate, and the exterior
bad-tail threshold estimate.  The conclusion is stated directly for
`ModelTruncatedOperator`, not for an abstract auxiliary operator.
-/

/-- A finite-scale real model operator has the expected weak-one conclusion
once a fiber CZ decomposition has supplied its pointwise good-plus-tail
bound and the three associated threshold budgets. -/
theorem scratch_ModelTruncatedOperator_fiberCZ_weakOne_of_data
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (f : ModelOperatorRealInput)
    (a b : ℝ) (good : E3 → ℝ) (tail : E3 → ℝ) (E : Set E3)
    (lambda : ℝ) (Aexception Agood Atail : ℝ≥0∞)
    (hpoint : ∀ x, x ∉ E →
      |ModelTruncatedOperator α u c f a b x| ≤ |good x| + tail x)
    (hexception : ENNReal.ofReal lambda * volume E ≤ Aexception)
    (hgood : ENNReal.ofReal (lambda / 2) *
      volume {x | lambda / 2 < |good x|} ≤ Agood)
    (htail : ENNReal.ofReal (lambda / 2) *
      volume ({x | lambda / 2 < tail x} ∩ Eᶜ) ≤ Atail) :
    ENNReal.ofReal lambda *
      volume {x | lambda < |ModelTruncatedOperator α u c f a b x|} ≤
        Aexception + 2 * Agood + 2 * Atail := by
  have hweak := scratch_fiberCZ_weakOne_assembly volume
    (fun x ↦ (ModelTruncatedOperator α u c f a b x : ℂ))
    (fun x ↦ (good x : ℂ)) tail E lambda
    (by
      intro x hx
      simpa only [Complex.norm_real, Real.norm_eq_abs] using hpoint x hx)
    Aexception Agood Atail hexception
    (by simpa only [Complex.norm_real, Real.norm_eq_abs] using hgood) htail
  simpa only [Complex.norm_real, Real.norm_eq_abs] using hweak

/-- The preceding weak-one consumer in the more literal CZ form: a pointwise
good/bad decomposition and a far-region bound for the bad contribution
produce the required outside good-plus-tail domination automatically. -/
theorem scratch_ModelTruncatedOperator_fiberCZ_weakOne_of_good_bad_data
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (f : ModelOperatorRealInput)
    (a b : ℝ) (good bad tail : E3 → ℝ) (E : Set E3)
    (lambda : ℝ) (Aexception Agood Atail : ℝ≥0∞)
    (hdecomp : ∀ x,
      ModelTruncatedOperator α u c f a b x = good x + bad x)
    (hbad : ∀ x, x ∉ E → |bad x| ≤ tail x)
    (hexception : ENNReal.ofReal lambda * volume E ≤ Aexception)
    (hgood : ENNReal.ofReal (lambda / 2) *
      volume {x | lambda / 2 < |good x|} ≤ Agood)
    (htail : ENNReal.ofReal (lambda / 2) *
      volume ({x | lambda / 2 < tail x} ∩ Eᶜ) ≤ Atail) :
    ENNReal.ofReal lambda *
      volume {x | lambda < |ModelTruncatedOperator α u c f a b x|} ≤
        Aexception + 2 * Agood + 2 * Atail := by
  apply scratch_ModelTruncatedOperator_fiberCZ_weakOne_of_data
    α u c f a b good tail E lambda Aexception Agood Atail
  · intro x hx
    rw [hdecomp x]
    calc
      |good x + bad x| ≤ |good x| + |bad x| := abs_add_le _ _
      _ ≤ |good x| + tail x := by
        simpa only [add_comm] using add_le_add_right (hbad x hx) |good x|
  · exact hexception
  · exact hgood
  · exact htail

end
end Twisted
end Auto
