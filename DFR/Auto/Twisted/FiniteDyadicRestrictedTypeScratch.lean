/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/
import Auto.Twisted.Twisted
import Auto.Twisted.TrilinearFiniteInterpolationScratch

namespace Auto
namespace Twisted

open scoped BigOperators

noncomputable section

/-! ### Finite dyadic restricted-type summation

This scratch isolates the summation core of a finite four-vertex
restricted-type proof.  A signed dyadic level is represented by a sign bit
and a nonnegative magnitude.  The sign split is deliberately explicit: it
allows finite simple functions to be decomposed into positive and negative
amplitude levels without any countability or convergence argument.
-/

/-- A signed dyadic amplitude level: the first coordinate records the sign
and the second its nonnegative dyadic height. -/
abbrev scratchSignedDyadicLevel := Fin 2 × ℕ

/-- A finite collection of signed dyadic levels has uniformly bounded
geometric mass.  This is the one-coordinate layer-cake summation used after
a strict restricted-type gain has supplied the factor `rho ^ magnitude`. -/
theorem scratch_sum_signedDyadic_geometric_le
    (rho : ℝ) (hrho0 : 0 ≤ rho) (hrho1 : rho < 1)
    (s : Finset scratchSignedDyadicLevel) :
    (∑ q ∈ s, rho ^ q.2) ≤ 2 * (1 - rho)⁻¹ := by
  classical
  let t : Finset (Fin 2 × ℕ) :=
    (Finset.univ : Finset (Fin 2)).product
      (s.image (fun q : scratchSignedDyadicLevel => q.2))
  have hsubset : s ⊆ t := by
    intro q hq
    have hqimage : q.2 ∈ s.image
        (fun r : scratchSignedDyadicLevel => r.2) :=
      Finset.mem_image.mpr ⟨q, hq, rfl⟩
    simpa [t] using hqimage
  have hnonneg : ∀ q ∈ t, q ∉ s → 0 ≤ rho ^ q.2 := by
    intro q _ _
    exact pow_nonneg hrho0 _
  calc
    (∑ q ∈ s, rho ^ q.2) ≤ ∑ q ∈ t, rho ^ q.2 :=
      Finset.sum_le_sum_of_subset_of_nonneg hsubset hnonneg
    _ = ∑ _e : Fin 2, ∑ n ∈ s.image
        (fun q : scratchSignedDyadicLevel => q.2), rho ^ n := by
      dsimp only [t]
      exact Finset.sum_product (Finset.univ : Finset (Fin 2))
        (s.image (fun q : scratchSignedDyadicLevel => q.2))
        (fun q => rho ^ q.2)
    _ ≤ ∑ _e : Fin 2, (1 - rho)⁻¹ := by
      apply Finset.sum_le_sum
      intro e he
      calc
        (∑ n ∈ s.image (fun q : scratchSignedDyadicLevel => q.2), rho ^ n) ≤
            ∑' n : ℕ, rho ^ n :=
          (summable_geometric_of_lt_one hrho0 hrho1).sum_le_tsum
            (s.image (fun q : scratchSignedDyadicLevel => q.2))
            (fun n _ ↦ pow_nonneg hrho0 n)
        _ = (1 - rho)⁻¹ := tsum_geometric_of_lt_one hrho0 hrho1
    _ = 2 * (1 - rho)⁻¹ := by
      norm_num [Fin.sum_univ_two]

/-- The exact factorization of the three independent dyadic level sums. -/
theorem scratch_sum_three_signedDyadic_geometric_eq
    (rho : ℝ) (s₁ s₂ s₃ : Finset scratchSignedDyadicLevel) :
    (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
      rho ^ (i.2 + j.2 + k.2)) =
      (∑ i ∈ s₁, rho ^ i.2) * (∑ j ∈ s₂, rho ^ j.2) *
        (∑ k ∈ s₃, rho ^ k.2) := by
  symm
  calc
    (∑ i ∈ s₁, rho ^ i.2) * (∑ j ∈ s₂, rho ^ j.2) *
        (∑ k ∈ s₃, rho ^ k.2) =
        (∑ i ∈ s₁, rho ^ i.2 * (∑ j ∈ s₂, rho ^ j.2)) *
          (∑ k ∈ s₃, rho ^ k.2) := by
            rw [← Finset.sum_mul]
    _ = (∑ i ∈ s₁, (∑ j ∈ s₂, rho ^ i.2 * rho ^ j.2)) *
          (∑ k ∈ s₃, rho ^ k.2) := by
            apply congrArg (fun x : ℝ => x * (∑ k ∈ s₃, rho ^ k.2))
            apply Finset.sum_congr rfl
            intro i hi
            rw [Finset.mul_sum]
    _ = ∑ i ∈ s₁, ∑ j ∈ s₂,
          (rho ^ i.2 * rho ^ j.2) * (∑ k ∈ s₃, rho ^ k.2) := by
            rw [Finset.sum_mul]
            apply Finset.sum_congr rfl
            intro i hi
            rw [Finset.sum_mul]
    _ = ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          (rho ^ i.2 * rho ^ j.2) * rho ^ k.2 := by
            apply Finset.sum_congr rfl
            intro i hi
            apply Finset.sum_congr rfl
            intro j hj
            rw [Finset.mul_sum]
    _ = ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          rho ^ (i.2 + j.2 + k.2) := by
            apply Finset.sum_congr rfl
            intro i hi
            apply Finset.sum_congr rfl
            intro j hj
            apply Finset.sum_congr rfl
            intro k hk
            rw [← pow_add, ← pow_add]

/-- The full finite three-input dyadic layer-cake.  Once a restricted-type
argument gives a strict gain `rho ^ (|n₁| + |n₂| + |n₃|)` on the signed
level triple, this uniformly sums all finite simple-function truncations. -/
theorem scratch_sum_three_signedDyadic_geometric_le
    (rho : ℝ) (hrho0 : 0 ≤ rho) (hrho1 : rho < 1)
    (s₁ s₂ s₃ : Finset scratchSignedDyadicLevel) :
    (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
      rho ^ (i.2 + j.2 + k.2)) ≤
      (2 * (1 - rho)⁻¹) ^ 3 := by
  let A : ℝ := ∑ i ∈ s₁, rho ^ i.2
  let B : ℝ := ∑ j ∈ s₂, rho ^ j.2
  let C : ℝ := ∑ k ∈ s₃, rho ^ k.2
  let K : ℝ := 2 * (1 - rho)⁻¹
  have hA : A ≤ K := scratch_sum_signedDyadic_geometric_le rho hrho0 hrho1 s₁
  have hB : B ≤ K := scratch_sum_signedDyadic_geometric_le rho hrho0 hrho1 s₂
  have hC : C ≤ K := scratch_sum_signedDyadic_geometric_le rho hrho0 hrho1 s₃
  have hA0 : 0 ≤ A := by
    dsimp only [A]
    exact Finset.sum_nonneg fun i hi => pow_nonneg hrho0 _
  have hB0 : 0 ≤ B := by
    dsimp only [B]
    exact Finset.sum_nonneg fun j hj => pow_nonneg hrho0 _
  have hC0 : 0 ≤ C := by
    dsimp only [C]
    exact Finset.sum_nonneg fun k hk => pow_nonneg hrho0 _
  have hK0 : 0 ≤ K := by
    dsimp only [K]
    exact mul_nonneg (by norm_num)
      (inv_nonneg.mpr (sub_nonneg.mpr hrho1.le))
  have hAB : A * B ≤ K * K := mul_le_mul hA hB hB0 hK0
  have hABC : A * B * C ≤ (K * K) * K :=
    mul_le_mul hAB hC hC0 (mul_nonneg hK0 hK0)
  calc
    (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
      rho ^ (i.2 + j.2 + k.2)) = A * B * C := by
        exact scratch_sum_three_signedDyadic_geometric_eq rho s₁ s₂ s₃
    _ ≤ (K * K) * K := hABC
    _ = K ^ 3 := by ring
    _ = (2 * (1 - rho)⁻¹) ^ 3 := by rfl

/-- A finite trilinear simple-function terminal.  It is deliberately stated
with the strict dyadic decay as a supplied atomwise hypothesis: deriving that
gain from the four restricted-type vertices is the genuine interpolation
step, while this theorem discharges all finite multilinearity and level-sum
bookkeeping once it is available. -/
theorem scratch_norm_trilinear_finset_sum_le_of_signedDyadicDecay
    {F G H : Type*} [AddCommMonoid F] [AddCommMonoid G] [AddCommMonoid H]
    [Module ℂ F] [Module ℂ G] [Module ℂ H]
    (B : F →ₗ[ℂ] G →ₗ[ℂ] H →ₗ[ℂ] ℂ)
    (s₁ s₂ s₃ : Finset scratchSignedDyadicLevel)
    (u : scratchSignedDyadicLevel → F)
    (v : scratchSignedDyadicLevel → G)
    (w : scratchSignedDyadicLevel → H)
    (a b d : scratchSignedDyadicLevel → ℂ)
    (C rho : ℝ) (hC : 0 ≤ C) (hrho0 : 0 ≤ rho) (hrho1 : rho < 1)
    (ha : ∀ i ∈ s₁, ‖a i‖ ≤ 1)
    (hb : ∀ j ∈ s₂, ‖b j‖ ≤ 1)
    (hd : ∀ k ∈ s₃, ‖d k‖ ≤ 1)
    (hatom : ∀ i ∈ s₁, ∀ j ∈ s₂, ∀ k ∈ s₃,
      ‖B (u i) (v j) (w k)‖ ≤ C * rho ^ (i.2 + j.2 + k.2)) :
    ‖B (∑ i ∈ s₁, a i • u i) (∑ j ∈ s₂, b j • v j)
        (∑ k ∈ s₃, d k • w k)‖ ≤
      C * (2 * (1 - rho)⁻¹) ^ 3 := by
  have hmain := scratch_norm_trilinear_finset_sum_le B s₁ s₂ s₃ u v w a b d
    (fun i j k => C * rho ^ (i.2 + j.2 + k.2)) hatom
  have hmajor_nonneg (i : scratchSignedDyadicLevel)
      (j : scratchSignedDyadicLevel) (k : scratchSignedDyadicLevel) :
      0 ≤ C * rho ^ (i.2 + j.2 + k.2) :=
    mul_nonneg hC (pow_nonneg hrho0 _)
  have hcoeff : ∀ i ∈ s₁, ∀ j ∈ s₂, ∀ k ∈ s₃,
      ‖a i‖ * ‖b j‖ * ‖d k‖ ≤ 1 := by
    intro i hi j hj k hk
    have hab : ‖a i‖ * ‖b j‖ ≤ (1 : ℝ) * 1 :=
      mul_le_mul (ha i hi) (hb j hj) (norm_nonneg _) (by norm_num)
    have habd : (‖a i‖ * ‖b j‖) * ‖d k‖ ≤ ((1 : ℝ) * 1) * 1 :=
      mul_le_mul hab (hd k hk) (norm_nonneg _) (by norm_num)
    simpa using habd
  have hsum_major :
      (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
        ‖a i‖ * ‖b j‖ * ‖d k‖ *
          (C * rho ^ (i.2 + j.2 + k.2))) ≤
        ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          C * rho ^ (i.2 + j.2 + k.2) := by
    apply Finset.sum_le_sum
    intro i hi
    apply Finset.sum_le_sum
    intro j hj
    apply Finset.sum_le_sum
    intro k hk
    have h := mul_le_mul_of_nonneg_right (hcoeff i hi j hj k hk)
      (hmajor_nonneg i j k)
    simpa only [one_mul] using h
  have hfactor :
      (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
        C * rho ^ (i.2 + j.2 + k.2)) =
        C * (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          rho ^ (i.2 + j.2 + k.2)) := by
    symm
    calc
      C * (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          rho ^ (i.2 + j.2 + k.2)) =
          ∑ i ∈ s₁, C * (∑ j ∈ s₂, ∑ k ∈ s₃,
            rho ^ (i.2 + j.2 + k.2)) := by
              rw [Finset.mul_sum]
      _ = ∑ i ∈ s₁, ∑ j ∈ s₂, C * (∑ k ∈ s₃,
          rho ^ (i.2 + j.2 + k.2)) := by
            apply Finset.sum_congr rfl
            intro i hi
            rw [Finset.mul_sum]
      _ = ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          C * rho ^ (i.2 + j.2 + k.2) := by
            apply Finset.sum_congr rfl
            intro i hi
            apply Finset.sum_congr rfl
            intro j hj
            rw [Finset.mul_sum]
  calc
    ‖B (∑ i ∈ s₁, a i • u i) (∑ j ∈ s₂, b j • v j)
        (∑ k ∈ s₃, d k • w k)‖ ≤
        ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          ‖a i‖ * ‖b j‖ * ‖d k‖ *
            (C * rho ^ (i.2 + j.2 + k.2)) := hmain
    _ ≤ ∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          C * rho ^ (i.2 + j.2 + k.2) := hsum_major
    _ = C * (∑ i ∈ s₁, ∑ j ∈ s₂, ∑ k ∈ s₃,
          rho ^ (i.2 + j.2 + k.2)) := hfactor
    _ ≤ C * (2 * (1 - rho)⁻¹) ^ 3 :=
      mul_le_mul_of_nonneg_left
        (scratch_sum_three_signedDyadic_geometric_le rho hrho0 hrho1 s₁ s₂ s₃) hC

end
end Twisted
end Auto
