/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory Filter
open scoped BigOperators ENNReal NNReal

noncomputable section

/-! ## Compact cone-localization cutoffs

These are the source's auxiliary `\widetilde\Phi` and
`\widetilde\Psi`, realized directly through `ContDiffBump`.  The chosen
inner and outer radii give exactly the plateau and support margins used by
the localized-symbol/Fourier-series step.
-/

/-- A smooth low-frequency cutoff equal to one on `[-2,2]` and zero outside
`(-3,3)`. -/
noncomputable def scratch_localConePhi (x : ℝ) : ℝ :=
  (⟨(2 : ℝ), 3, by norm_num, by norm_num⟩ : ContDiffBump (0 : ℝ)) x

/-- One positive annular cutoff, equal to one on `[1,2]` and supported in
`(1/2,5/2)`. -/
noncomputable def scratch_localConePsiPos (x : ℝ) : ℝ :=
  (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ : ContDiffBump (3 / 2 : ℝ)) x

/-- The corresponding negative annular cutoff. -/
noncomputable def scratch_localConePsiNeg (x : ℝ) : ℝ :=
  (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ : ContDiffBump (-3 / 2 : ℝ)) x

/-- A smooth even annular cutoff equal to one on
`[-2,-1] ∪ [1,2]` and zero on `[-1/2,1/2]` and outside
`(-5/2,5/2)`. -/
noncomputable def scratch_localConePsi (x : ℝ) : ℝ :=
  scratch_localConePsiPos x + scratch_localConePsiNeg x

private theorem scratch_localConePhi_apply (x : ℝ) :
    scratch_localConePhi x =
      (⟨(2 : ℝ), 3, by norm_num, by norm_num⟩ : ContDiffBump (0 : ℝ)) x := by
  rfl

private theorem scratch_localConePsiPos_apply (x : ℝ) :
    scratch_localConePsiPos x =
      (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
        ContDiffBump (3 / 2 : ℝ)) x := by
  rfl

private theorem scratch_localConePsiNeg_apply (x : ℝ) :
    scratch_localConePsiNeg x =
      (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
        ContDiffBump (-3 / 2 : ℝ)) x := by
  rfl

theorem scratch_localConePhi_nonneg (x : ℝ) : 0 ≤ scratch_localConePhi x := by
  rw [scratch_localConePhi_apply]
  exact ContDiffBump.nonneg' _ _

theorem scratch_localConePhi_le_one (x : ℝ) : scratch_localConePhi x ≤ 1 := by
  rw [scratch_localConePhi_apply]
  exact ContDiffBump.le_one _

theorem scratch_localConePhi_eq_one {x : ℝ} (hx : |x| ≤ 2) :
    scratch_localConePhi x = 1 := by
  rw [scratch_localConePhi_apply]
  apply ContDiffBump.one_of_mem_closedBall
  rw [Metric.mem_closedBall, Real.dist_eq]
  simpa using hx

theorem scratch_localConePhi_eq_zero {x : ℝ} (hx : 3 ≤ |x|) :
    scratch_localConePhi x = 0 := by
  rw [scratch_localConePhi_apply]
  apply ContDiffBump.zero_of_le_dist
  rw [Real.dist_eq]
  simpa using hx

theorem scratch_localConePhi_contDiff :
    ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞) scratch_localConePhi := by
  rw [show scratch_localConePhi =
      (⟨(2 : ℝ), 3, by norm_num, by norm_num⟩ : ContDiffBump (0 : ℝ)) by rfl]
  exact (⟨(2 : ℝ), 3, by norm_num, by norm_num⟩ : ContDiffBump (0 : ℝ)).contDiff

theorem scratch_localConePsiPos_nonneg (x : ℝ) :
    0 ≤ scratch_localConePsiPos x := by
  rw [scratch_localConePsiPos_apply]
  exact ContDiffBump.nonneg' _ _

theorem scratch_localConePsiNeg_nonneg (x : ℝ) :
    0 ≤ scratch_localConePsiNeg x := by
  rw [scratch_localConePsiNeg_apply]
  exact ContDiffBump.nonneg' _ _

theorem scratch_localConePsiPos_eq_one {x : ℝ}
    (hx : |x - 3 / 2| ≤ 1 / 2) : scratch_localConePsiPos x = 1 := by
  rw [scratch_localConePsiPos_apply]
  apply ContDiffBump.one_of_mem_closedBall
  rw [Metric.mem_closedBall, Real.dist_eq]
  simpa using hx

theorem scratch_localConePsiNeg_eq_one {x : ℝ}
    (hx : |x + 3 / 2| ≤ 1 / 2) : scratch_localConePsiNeg x = 1 := by
  rw [scratch_localConePsiNeg_apply]
  apply ContDiffBump.one_of_mem_closedBall
  rw [Metric.mem_closedBall, Real.dist_eq]
  convert hx using 1 <;> ring

theorem scratch_localConePsiPos_eq_zero {x : ℝ}
    (hx : 1 ≤ |x - 3 / 2|) : scratch_localConePsiPos x = 0 := by
  rw [scratch_localConePsiPos_apply]
  apply ContDiffBump.zero_of_le_dist
  rw [Real.dist_eq]
  simpa using hx

theorem scratch_localConePsiNeg_eq_zero {x : ℝ}
    (hx : 1 ≤ |x + 3 / 2|) : scratch_localConePsiNeg x = 0 := by
  rw [scratch_localConePsiNeg_apply]
  apply ContDiffBump.zero_of_le_dist
  rw [Real.dist_eq]
  convert hx using 1 <;> ring

theorem scratch_localConePsi_nonneg (x : ℝ) : 0 ≤ scratch_localConePsi x :=
  add_nonneg (scratch_localConePsiPos_nonneg x) (scratch_localConePsiNeg_nonneg x)

theorem scratch_localConePsi_eq_one_of_one_le {x : ℝ}
    (hlo : 1 ≤ x) (hhi : x ≤ 2) : scratch_localConePsi x = 1 := by
  unfold scratch_localConePsi
  have hpos : |x - 3 / 2| ≤ 1 / 2 := by
    rw [abs_le]
    constructor <;> linarith
  have hneg : 1 ≤ |x + 3 / 2| := by
    rw [abs_of_nonneg (by linarith)]
    linarith
  rw [scratch_localConePsiPos_eq_one hpos, scratch_localConePsiNeg_eq_zero hneg]
  ring

theorem scratch_localConePsi_eq_one_of_neg {x : ℝ}
    (hlo : -2 ≤ x) (hhi : x ≤ -1) : scratch_localConePsi x = 1 := by
  unfold scratch_localConePsi
  have hneg : |x + 3 / 2| ≤ 1 / 2 := by
    rw [abs_le]
    constructor <;> linarith
  have hpos : 1 ≤ |x - 3 / 2| := by
    rw [abs_of_nonpos (by linarith)]
    linarith
  rw [scratch_localConePsiNeg_eq_one hneg, scratch_localConePsiPos_eq_zero hpos]
  ring

theorem scratch_localConePsi_eq_zero_of_abs_le_half {x : ℝ}
    (hx : |x| ≤ 1 / 2) : scratch_localConePsi x = 0 := by
  unfold scratch_localConePsi
  have hpos : 1 ≤ |x - 3 / 2| := by
    rw [abs_of_nonpos (by
      have := (abs_le.mp hx).2
      linarith)]
    have h := (abs_le.mp hx).2
    linarith
  have hneg : 1 ≤ |x + 3 / 2| := by
    rw [abs_of_nonneg (by
      have := (abs_le.mp hx).1
      linarith)]
    have h := (abs_le.mp hx).1
    linarith
  rw [scratch_localConePsiPos_eq_zero hpos, scratch_localConePsiNeg_eq_zero hneg]
  ring

theorem scratch_localConePsi_eq_zero_of_five_halves_le_abs {x : ℝ}
    (hx : 5 / 2 ≤ |x|) : scratch_localConePsi x = 0 := by
  unfold scratch_localConePsi
  rcases le_total 0 x with hx0 | hx0
  · have hx' : 5 / 2 ≤ x := by
      rwa [abs_of_nonneg hx0] at hx
    have hpos : 1 ≤ |x - 3 / 2| := by
      rw [abs_of_nonneg (by linarith)]
      linarith
    have hneg : 1 ≤ |x + 3 / 2| := by
      rw [abs_of_nonneg (by linarith)]
      linarith
    rw [scratch_localConePsiPos_eq_zero hpos, scratch_localConePsiNeg_eq_zero hneg]
    ring
  · have hx' : x ≤ -5 / 2 := by
      rw [abs_of_nonpos hx0] at hx
      linarith
    have hpos : 1 ≤ |x - 3 / 2| := by
      rw [abs_of_nonpos (by linarith)]
      linarith
    have hneg : 1 ≤ |x + 3 / 2| := by
      rw [abs_of_nonpos (by linarith)]
      linarith
    rw [scratch_localConePsiPos_eq_zero hpos, scratch_localConePsiNeg_eq_zero hneg]
    ring

theorem scratch_localConePsi_contDiff :
    ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞) scratch_localConePsi := by
  unfold scratch_localConePsi
  apply ContDiff.add
  · rw [show scratch_localConePsiPos =
      (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
        ContDiffBump (3 / 2 : ℝ)) by rfl]
    exact (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
      ContDiffBump (3 / 2 : ℝ)).contDiff
  · rw [show scratch_localConePsiNeg =
      (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
        ContDiffBump (-3 / 2 : ℝ)) by rfl]
    exact (⟨(1 / 2 : ℝ), 1, by norm_num, by norm_num⟩ :
      ContDiffBump (-3 / 2 : ℝ)).contDiff

/-- The auxiliary low cutoff is identically one wherever the original
Calderón low cutoff can be nonzero. -/
theorem scratch_localConePhi_eq_one_of_conePhi_ne_zero {x : ℝ}
    (hx : conePhi x ≠ 0) : scratch_localConePhi x = 1 := by
  apply scratch_localConePhi_eq_one
  apply le_of_lt
  apply lt_of_not_ge
  intro hlarge
  exact hx (conePhi_eq_zero_of_two_le_abs hlarge)

/-- The auxiliary annular cutoff is identically one wherever the original
Calderón annular cutoff can be nonzero. -/
theorem scratch_localConePsi_eq_one_of_conePsi_ne_zero {x : ℝ}
    (hx : conePsi x ≠ 0) : scratch_localConePsi x = 1 := by
  have hlow : 1 < |x| := by
    apply lt_of_not_ge
    intro hsmall
    exact hx (conePsi_eq_zero_of_abs_le_one hsmall)
  have hhigh : |x| < 2 := by
    apply lt_of_not_ge
    intro hlarge
    exact hx (conePsi_eq_zero_of_two_le_abs hlarge)
  rcases le_total 0 x with hx0 | hx0
  · have hlow' : 1 < x := by rwa [abs_of_nonneg hx0] at hlow
    have hhigh' : x < 2 := by rwa [abs_of_nonneg hx0] at hhigh
    exact scratch_localConePsi_eq_one_of_one_le hlow'.le hhigh'.le
  · apply scratch_localConePsi_eq_one_of_neg
      (by
        rw [abs_of_nonpos hx0] at hhigh
        linarith)
      (by
        rw [abs_of_nonpos hx0] at hlow
        linarith)

/-- The real auxiliary localization factor used to divide out the passive
Gaussians on the three cones. -/
noncomputable def scratch_localizedCutoff (i : Fin 3) (η : E3) : ℝ :=
  scratch_localConePsi (η i) *
    ∏ j : Fin 3,
      if j = i then 1 else scratch_localConePhi (η j) / gaussian (η j)

/-- The complex localized multiplier before periodic extension. -/
noncomputable def scratch_localizedSymbol
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (η : E3) : ℂ :=
  m (α.dilate t⁻¹ η) * scratch_localizedCutoff i η

@[simp] theorem scratch_localizedSymbol_apply
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (η : E3) :
    scratch_localizedSymbol α m i t η =
      m (α.dilate t⁻¹ η) * scratch_localizedCutoff i η := rfl

/-- A nonzero localized cutoff is separated from the singular origin by its
active annular coordinate. -/
theorem scratch_abs_active_gt_half_of_localizedCutoff_ne_zero
    (i : Fin 3) (η : E3) (hη : scratch_localizedCutoff i η ≠ 0) :
    1 / 2 < |η i| := by
  apply lt_of_not_ge
  intro hsmall
  have hpsi : scratch_localConePsi (η i) = 0 :=
    scratch_localConePsi_eq_zero_of_abs_le_half hsmall
  apply hη
  simp [scratch_localizedCutoff, hpsi]

theorem scratch_localizedCutoff_ne_zero_implies_ne_zero
    (i : Fin 3) (η : E3) (hη : scratch_localizedCutoff i η ≠ 0) : η ≠ 0 := by
  intro hzero
  have hactive := scratch_abs_active_gt_half_of_localizedCutoff_ne_zero i η hη
  rw [hzero] at hactive
  norm_num at hactive

/-- The cutoff is identically zero on a fixed ball around the singular
frequency origin. -/
theorem scratch_localizedCutoff_eq_zero_of_norm_lt_half
    (i : Fin 3) (η : E3) (hη : ‖η‖ < 1 / 2) :
    scratch_localizedCutoff i η = 0 := by
  have hcoord : |η i| ≤ ‖η‖ := by
    simpa only [Real.norm_eq_abs] using PiLp.norm_apply_le η i
  have hpsi : scratch_localConePsi (η i) = 0 :=
    scratch_localConePsi_eq_zero_of_abs_le_half (by linarith)
  simp [scratch_localizedCutoff, hpsi]

theorem scratch_localizedSymbol_eq_zero_of_norm_lt_half
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (η : E3)
    (hη : ‖η‖ < 1 / 2) : scratch_localizedSymbol α m i t η = 0 := by
  simp [scratch_localizedSymbol,
    scratch_localizedCutoff_eq_zero_of_norm_lt_half i η hη]

/-- On the source cone factors, the auxiliary cutoff divides out precisely
the passive Gaussian factors. -/
theorem scratch_localizedCutoff_eq_passiveGaussianInv
    (i : Fin 3) (η : E3)
    (hpsi : conePsi (η i) ≠ 0)
    (hphi : ∀ j : Fin 3, j ≠ i → conePhi (η j) ≠ 0) :
    scratch_localizedCutoff i η =
      ∏ j : Fin 3, if j = i then 1 else (gaussian (η j))⁻¹ := by
  unfold scratch_localizedCutoff
  rw [scratch_localConePsi_eq_one_of_conePsi_ne_zero hpsi]
  simp only [one_mul]
  apply Finset.prod_congr rfl
  intro j _
  by_cases hji : j = i
  · simp [hji]
  · rw [if_neg hji]
    rw [scratch_localConePhi_eq_one_of_conePhi_ne_zero (hphi j hji)]
    rw [one_div]
    simp only [if_neg hji]

/-- Each coordinate outside the auxiliary box kills the localized cutoff.
The active coordinate has the slightly smaller annular support, while a
passive coordinate is killed by the low cutoff. -/
theorem scratch_localizedCutoff_eq_zero_of_coordinate_large
    (i j : Fin 3) (η : E3)
    (hj : 3 ≤ |η j|) : scratch_localizedCutoff i η = 0 := by
  by_cases hji : j = i
  · subst j
    have hpsi : scratch_localConePsi (η i) = 0 :=
      scratch_localConePsi_eq_zero_of_five_halves_le_abs (by linarith)
    simp [scratch_localizedCutoff, hpsi]
  · have hphi : scratch_localConePhi (η j) = 0 :=
      scratch_localConePhi_eq_zero hj
    unfold scratch_localizedCutoff
    have hterm : (if j = i then 1 else
        scratch_localConePhi (η j) / gaussian (η j)) = 0 := by
      simp [hji, hphi]
    have hprod :
        (∏ k : Fin 3, if k = i then (1 : ℝ) else
          scratch_localConePhi (η k) / gaussian (η k)) = 0 := by
      exact Finset.prod_eq_zero (Finset.mem_univ j) hterm
    simp [hprod]

/-- The auxiliary cone cutoff has compact support in the fixed frequency
box.  This gives uniform derivative bounds after differentiating it. -/
theorem scratch_localizedCutoff_hasCompactSupport (i : Fin 3) :
    HasCompactSupport (scratch_localizedCutoff i) := by
  apply HasCompactSupport.intro (isCompact_closedBall (0 : E3) 9)
  intro η hη
  have hnorm : 9 < ‖η‖ := by
    simpa only [Metric.mem_closedBall, dist_zero_right, not_le] using hη
  have hlarge : ∃ j : Fin 3, 3 < |η j| := by
    by_contra h
    push_neg at h
    have hsum : |η 0| + |η 1| + |η 2| ≤ 9 := by
      nlinarith [h 0, h 1, h 2]
    linarith [norm_le_sum_abs η]
  obtain ⟨j, hj⟩ := hlarge
  exact scratch_localizedCutoff_eq_zero_of_coordinate_large i j η hj.le

/-- Smoothness of the fixed localized cutoff.  The Gaussian never vanishes,
so dividing it out in passive coordinates preserves smoothness. -/
theorem scratch_localizedCutoff_contDiff (i : Fin 3) :
    ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞) (scratch_localizedCutoff i) := by
  have hpsi : ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞)
      (fun η : E3 ↦ scratch_localConePsi (η i)) :=
    scratch_localConePsi_contDiff.comp (coordinateProjection i).contDiff
  have hfactor : ∀ j : Fin 3, ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞)
      (fun η : E3 ↦ if j = i then 1 else
        scratch_localConePhi (η j) / gaussian (η j)) := by
    intro j
    by_cases hji : j = i
    · subst j
      simpa using
        (contDiff_const : ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞)
          (fun _ : E3 ↦ (1 : ℝ)))
    · simp only [if_neg hji]
      exact
        (scratch_localConePhi_contDiff.comp (coordinateProjection j).contDiff).div
          (gaussian_contDiff.comp (coordinateProjection j).contDiff)
          (fun η ↦ ne_of_gt (gaussian_pos (η j)))
  unfold scratch_localizedCutoff
  exact hpsi.mul (contDiff_prod (fun j _ ↦ hfactor j))

/-- A single constant bounds all localized-cutoff derivatives through any
fixed order.  It is independent of the cone, frequency point, multiplier,
and dilation scale. -/
theorem scratch_exists_localizedCutoff_derivative_bound
    (i : Fin 3) (N : ℕ) :
    ∃ C : ℝ, 1 ≤ C ∧ ∀ n ≤ N, ∀ η : E3,
      ‖iteratedFDeriv ℝ n (scratch_localizedCutoff i) η‖ ≤ C := by
  have hb : ∀ n : ℕ, ∃ C : ℝ, ∀ η : E3,
      ‖iteratedFDeriv ℝ n (scratch_localizedCutoff i) η‖ ≤ C := by
    intro n
    exact ((scratch_localizedCutoff_hasCompactSupport i).iteratedFDeriv n).exists_bound_of_continuous
      ((scratch_localizedCutoff_contDiff i).continuous_iteratedFDeriv
        (by exact_mod_cast le_top))
  choose C hC using hb
  have hnn : (0 : ℝ) ≤ ∑ n ∈ Finset.range (N + 1), |C n| :=
    Finset.sum_nonneg (fun n _ ↦ abs_nonneg _)
  refine ⟨1 + ∑ n ∈ Finset.range (N + 1), |C n|, by linarith, ?_⟩
  intro n hn η
  refine le_trans (hC n η) ?_
  have hmem : n ∈ Finset.range (N + 1) := Finset.mem_range.mpr (by omega)
  have hle : |C n| ≤ ∑ m ∈ Finset.range (N + 1), |C m| :=
    Finset.single_le_sum (f := fun m ↦ |C m|) (fun m _ ↦ abs_nonneg _) hmem
  linarith [le_abs_self (C n)]

/-- Despite the multiplier only being smooth off zero, multiplication by the
localized cutoff gives a globally smooth localized symbol: the cutoff is
identically zero in a neighborhood of the only singular point. -/
theorem scratch_localizedSymbol_contDiff
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    ContDiff ℝ 110 (scratch_localizedSymbol α m i t) := by
  rw [contDiff_iff_contDiffAt]
  intro η
  by_cases hη : η = 0
  · subst η
    refine (contDiffAt_const : ContDiffAt ℝ 110 (fun _ : E3 ↦ (0 : ℂ)) 0).congr_of_eventuallyEq ?_
    filter_upwards [Metric.ball_mem_nhds (0 : E3) (by norm_num : (0 : ℝ) < 1 / 2)] with z hz
    have hnorm : ‖z‖ < 1 / 2 := by
      simpa only [Metric.mem_ball, dist_zero_right] using hz
    exact scratch_localizedSymbol_eq_zero_of_norm_lt_half α m i t z hnorm
  · let L : E3 →L[ℝ] E3 :=
      { α.dilateLinear t⁻¹ with
        cont := LinearMap.continuous_of_finiteDimensional (α.dilateLinear t⁻¹) }
    have hLapply (z : E3) : L z = α.dilate t⁻¹ z := by
      change (α.dilateLinear t⁻¹) z = α.dilate t⁻¹ z
      exact α.dilateLinear_apply t⁻¹ z
    have hLηne : L η ≠ 0 := by
      rw [hLapply]
      intro hzero
      have hrecover := α.dilate_apply_inv ht.ne' η
      rw [hzero] at hrecover
      apply hη
      change α.dilate t (0 : E3) = η at hrecover
      have hzeroDilate : α.dilate t (0 : E3) = 0 := by
        rw [← α.dilateLinear_apply]
        exact (α.dilateLinear t).map_zero
      rw [hzeroDilate] at hrecover
      exact hrecover.symm
    have hopen : IsOpen ({0}ᶜ : Set E3) := by
      simpa using (isOpen_compl_singleton : IsOpen ({0}ᶜ : Set E3))
    have hcompOn : ContDiffOn ℝ 110 (m ∘ L) (L ⁻¹' ({0}ᶜ : Set E3)) :=
      hm.2.2.2.1.comp_continuousLinearMap L
    have hcompAt : ContDiffAt ℝ 110 (m ∘ L) η :=
      hcompOn.contDiffAt ((hopen.preimage L.continuous).mem_nhds (by
        simpa using hLηne))
    have hcutAt : ContDiffAt ℝ 110 (scratch_localizedCutoff i) η :=
      ((scratch_localizedCutoff_contDiff i).of_le
        (show (110 : WithTop ℕ∞) ≤ ((↑(⊤ : ℕ∞)) : WithTop ℕ∞) by
          exact WithTop.coe_le_coe.mpr (show (110 : ℕ∞) ≤ ⊤ by exact le_top))).contDiffAt
    have hcutAtC : ContDiffAt ℝ 110
        (fun z : E3 ↦ (scratch_localizedCutoff i z : ℂ)) η := by
      have hglobal : ContDiff ℝ ((⊤ : ℕ∞) : WithTop ℕ∞)
          (Complex.ofRealCLM ∘ scratch_localizedCutoff i) :=
        Complex.ofRealCLM.contDiff.comp (scratch_localizedCutoff_contDiff i)
      exact ((hglobal.of_le
        (show (110 : WithTop ℕ∞) ≤ ((↑(⊤ : ℕ∞)) : WithTop ℕ∞) by
          exact WithTop.coe_le_coe.mpr (show (110 : ℕ∞) ≤ ⊤ by exact le_top))).contDiffAt)
    have heq : scratch_localizedSymbol α m i t =
        fun z : E3 ↦ (m ∘ L) z * scratch_localizedCutoff i z := by
      funext z
      simp only [scratch_localizedSymbol, Function.comp_apply]
      rw [hLapply]
    rw [heq]
    exact hcompAt.mul hcutAtC

end
end Twisted
end Auto
