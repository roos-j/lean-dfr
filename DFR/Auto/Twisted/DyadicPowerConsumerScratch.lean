/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory
open scoped ENNReal

noncomputable section

/-! ## Consumers of a fixed-scale dyadic majorant -/

/-- A fixed-scale majorant automatically majorizes the literal all-positive-
scales second maximal power. -/
theorem scratch_anisotropicMaximalTwoPower_le_scaledDyadic_of_fixedScale
    (α : Anisotropy) (f : SchwartzMap E3 ℝ) (K : ℝ)
    (hfixed : ∀ (t : ℝ), 0 < t → ∀ p : E3,
      cubeSecondMoment α t p f ≤
        ‖scaledIteratedCoordinateDyadicBallMaximal K
          (complexifyRealSchwartz f) p‖)
    (p : E3) :
    anisotropicMaximalTwoPower α f p ≤
      ‖scaledIteratedCoordinateDyadicBallMaximal K
        (complexifyRealSchwartz f) p‖ := by
  unfold anisotropicMaximalTwoPower
  apply csSup_le
  · exact ⟨cubeSecondMoment α 1 p f, ⟨1, by norm_num, rfl⟩⟩
  · rintro a ⟨t, ht, rfl⟩
    exact hfixed t ht p

/-- The identical all-scale closure for the fourth maximal power. -/
theorem scratch_anisotropicMaximalFourPower_le_scaledDyadic_of_fixedScale
    (α : Anisotropy) (f : SchwartzMap E3 ℝ) (K : ℝ)
    (hfixed : ∀ (t : ℝ), 0 < t → ∀ p : E3,
      fourthMoment α t p f ≤
        ‖scaledIteratedCoordinateDyadicBallMaximal K
          (complexifyRealSchwartz f) p‖)
    (p : E3) :
    anisotropicMaximalFourPower α f p ≤
      ‖scaledIteratedCoordinateDyadicBallMaximal K
        (complexifyRealSchwartz f) p‖ := by
  unfold anisotropicMaximalFourPower
  apply csSup_le
  · exact ⟨fourthMoment α 1 p f, ⟨1, by norm_num, rfl⟩⟩
  · rintro a ⟨t, ht, rfl⟩
    exact hfixed t ht p

/-- The fully general Schwartz dyadic terminal can be converted to any
positive real power by Mathlib's `MemLp.norm_rpow_div` interface.  This is
the exact `q/s` to `q` conversion used for the second and fourth roots. -/
theorem scratch_scaledDyadic_memLp_norm_rpow_div_of_realSchwartz
    (K : ℝ) (hK : 0 ≤ K) (f : SchwartzMap E3 ℝ)
    {p : ℝ} (hp : 1 < p) (s : ℝ≥0∞) :
    MemLp (fun x : E3 ↦
      ‖scaledIteratedCoordinateDyadicBallMaximal K
        (complexifyRealSchwartz f) x‖ ^ s.toReal)
      (ENNReal.ofReal p / s) volume := by
  exact (scaledIteratedCoordinateDyadicBallMaximal_memLp_of_realSchwartz
    K hK f hp).norm_rpow_div s

end

end Twisted
end Auto
