/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted
import Mathlib.Analysis.Fourier.AddCircleMulti

namespace Auto.Twisted

open MeasureTheory Filter TopologicalSpace
open scoped BigOperators ENNReal Topology

noncomputable section

abbrev Circle8 := AddCircle (8 : ℝ)

local instance : Fact (0 < (8 : ℝ)) := ⟨by norm_num⟩

/-- Lift a function that is period-eight in its first real coordinate to the
first additive-circle coordinate. -/
noncomputable def scratch_periodizeFirst
    (f : ℝ → ℝ → ℝ → ℂ)
    (hper : ∀ y z : ℝ, Function.Periodic (fun x : ℝ ↦ f x y z) 8) :
    Circle8 → ℝ → ℝ → ℂ :=
  fun x y z ↦ (hper y z).lift x

@[simp]
theorem scratch_periodizeFirst_coe
    (f : ℝ → ℝ → ℝ → ℂ)
    (hper : ∀ y z : ℝ, Function.Periodic (fun x : ℝ ↦ f x y z) 8)
    (x y z : ℝ) :
    scratch_periodizeFirst f hper (x : Circle8) y z = f x y z := by
  simp [scratch_periodizeFirst]

/-- The first quotient lift is continuous whenever the raw three-variable
function is continuous. -/
theorem scratch_continuous_periodizeFirst
    (f : ℝ → ℝ → ℝ → ℂ)
    (hper : ∀ y z : ℝ, Function.Periodic (fun x : ℝ ↦ f x y z) 8)
    (hcont : Continuous (fun p : ℝ × (ℝ × ℝ) ↦ f p.1 p.2.1 p.2.2)) :
    Continuous (fun p : Circle8 × (ℝ × ℝ) ↦
      scratch_periodizeFirst f hper p.1 p.2.1 p.2.2) := by
  rw [← (QuotientAddGroup.isOpenQuotientMap_mk.prodMap
    IsOpenQuotientMap.id).continuous_comp_iff]
  change Continuous (fun p : ℝ × (ℝ × ℝ) ↦
    scratch_periodizeFirst f hper (p.1 : Circle8) p.2.1 p.2.2)
  simpa only [scratch_periodizeFirst_coe] using hcont

/-- After quotienting the first coordinate, periodicity in the second
coordinate is inherited exactly. -/
theorem scratch_periodizeFirst_periodic_second
    (f : ℝ → ℝ → ℝ → ℂ)
    (hper0 : ∀ y z : ℝ, Function.Periodic (fun x : ℝ ↦ f x y z) 8)
    (hper1 : ∀ x z : ℝ, Function.Periodic (fun y : ℝ ↦ f x y z) 8)
    (x : Circle8) (z : ℝ) :
    Function.Periodic (fun y : ℝ ↦ scratch_periodizeFirst f hper0 x y z) 8 := by
  intro y
  induction x using QuotientAddGroup.induction_on with
  | _ x =>
    simpa only [scratch_periodizeFirst_coe] using hper1 x z y

/-- A parameterized interval lift to the period-eight additive circle. -/
noncomputable def scratch_liftIcoParam {X : Type*}
    (f : ℝ → X → ℂ) : Circle8 → X → ℂ :=
  fun q x ↦ AddCircle.liftIco 8 (-4) (fun r : ℝ ↦ f r x) q

theorem scratch_continuous_liftIcoParam
    {X : Type*} [TopologicalSpace X] [LocallyCompactSpace X]
    (f : ℝ → X → ℂ)
    (hboundary : ∀ x : X, f (-4) x = f 4 x)
    (hcont : Continuous (fun p : ℝ × X ↦ f p.1 p.2)) :
    Continuous (fun p : Circle8 × X ↦ scratch_liftIcoParam f p.1 p.2) := by
  let F : C(ℝ × X, ℂ) := ⟨fun p ↦ f p.1 p.2, hcont⟩
  let G : C(ℝ, C(X, ℂ)) := ContinuousMap.curry F
  have hGboundary : G (-4) = G 4 := by
    ext x
    exact hboundary x
  let H : C(Circle8, C(X, ℂ)) :=
    ⟨AddCircle.liftIco 8 (-4) G,
      AddCircle.liftIco_continuous (by
        norm_num
        exact hGboundary) G.continuous.continuousOn⟩
  have hunc : Continuous (fun p : Circle8 × X ↦ H p.1 p.2) :=
    ContinuousMap.continuous_uncurry_of_continuous H
  refine hunc.congr ?_
  intro p
  rfl

/-- If every raw section is zero, its circle lift is zero. -/
theorem scratch_liftIcoParam_eq_zero
    {X : Type*} (f : ℝ → X → ℂ) (q : Circle8) (x : X)
    (hzero : ∀ r : ℝ, f r x = 0) : scratch_liftIcoParam f q x = 0 := by
  unfold scratch_liftIcoParam
  change f (↑(AddCircle.equivIco 8 (-4) q) : ℝ) x = 0
  exact hzero _

theorem scratch_liftIcoParam_coe
    {X : Type*} (f : ℝ → X → ℂ) {r : ℝ}
    (hr : r ∈ Set.Ico (-4 : ℝ) 4) (x : X) :
    scratch_liftIcoParam f (r : Circle8) x = f r x := by
  have hr' : r ∈ Set.Ico (-4 : ℝ) ((-4 : ℝ) + 8) := by
    norm_num
    exact hr
  unfold scratch_liftIcoParam
  exact AddCircle.liftIco_coe_apply hr'

/-- The localized symbol in ordinary three scalar coordinates. -/
noncomputable def scratch_localizedSymbolRaw
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x y z : ℝ) : ℂ :=
  localizedSymbol α m i t (frequencyAssemble3 x y z)

@[simp]
theorem scratch_localizedSymbolRaw_apply
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x y z : ℝ) :
    scratch_localizedSymbolRaw α m i t x y z =
      localizedSymbol α m i t (frequencyAssemble3 x y z) := rfl

theorem scratch_continuous_localizedSymbolRaw
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (fun p : ℝ × (ℝ × ℝ) ↦
      scratch_localizedSymbolRaw α m i t p.1 p.2.1 p.2.2) := by
  have hcoordinates : Continuous (fun p : ℝ × (ℝ × ℝ) ↦
      frequencyAssemble3 p.1 p.2.1 p.2.2) := by
    change Continuous (WithLp.toLp 2 ∘ fun p : ℝ × (ℝ × ℝ) ↦
      ![p.1, p.2.1, p.2.2])
    apply (PiLp.continuous_toLp 2 (fun _ : Fin 3 ↦ ℝ)).comp
    apply continuous_pi
    intro j
    fin_cases j <;> fun_prop
  exact (localizedSymbol_contDiff α M m hm i ht).continuous.comp hcoordinates

theorem scratch_localizedSymbolRaw_eq_zero_of_coordinate_large
    (α : Anisotropy) (m : E3 → ℂ) (i j : Fin 3) (t x y z : ℝ)
    (hj : 3 ≤ |![x, y, z] j|) :
    scratch_localizedSymbolRaw α m i t x y z = 0 := by
  rw [scratch_localizedSymbolRaw_apply]
  apply localizedSymbol_eq_zero_of_coordinate_large α m i j t
  simpa only [frequencyAssemble3_apply] using hj

theorem scratch_localizedSymbolRaw_zero_first_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t y z : ℝ) :
    scratch_localizedSymbolRaw α m i t (-4) y z = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 0 t
  norm_num

theorem scratch_localizedSymbolRaw_zero_first_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t y z : ℝ) :
    scratch_localizedSymbolRaw α m i t 4 y z = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 0 t
  norm_num

theorem scratch_localizedSymbolRaw_zero_second_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x z : ℝ) :
    scratch_localizedSymbolRaw α m i t x (-4) z = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 1 t
  norm_num

theorem scratch_localizedSymbolRaw_zero_second_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x z : ℝ) :
    scratch_localizedSymbolRaw α m i t x 4 z = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 1 t
  norm_num

theorem scratch_localizedSymbolRaw_zero_third_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x y : ℝ) :
    scratch_localizedSymbolRaw α m i t x y (-4) = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 2 t
  change 3 ≤ |(-4 : ℝ)|
  norm_num

theorem scratch_localizedSymbolRaw_zero_third_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t x y : ℝ) :
    scratch_localizedSymbolRaw α m i t x y 4 = 0 := by
  apply scratch_localizedSymbolRaw_eq_zero_of_coordinate_large α m i 2 t
  change 3 ≤ |(4 : ℝ)|
  norm_num

/-- The first coordinate of the compactly supported localized symbol, lifted
to the period-eight additive circle. -/
noncomputable def scratch_periodizedLocalizedFirst
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) :
    Circle8 → ℝ → ℝ → ℂ :=
  fun q y z ↦ scratch_liftIcoParam
    (fun x (yz : ℝ × ℝ) ↦ scratch_localizedSymbolRaw α m i t x yz.1 yz.2)
    q (y, z)

theorem scratch_continuous_periodizedLocalizedFirst
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (fun p : Circle8 × (ℝ × ℝ) ↦
      scratch_periodizedLocalizedFirst α m i t p.1 p.2.1 p.2.2) := by
  simpa only [scratch_periodizedLocalizedFirst] using
    (scratch_continuous_liftIcoParam
      (fun x (yz : ℝ × ℝ) ↦ scratch_localizedSymbolRaw α m i t x yz.1 yz.2)
      (by
        intro yz
        rw [scratch_localizedSymbolRaw_zero_first_neg,
          scratch_localizedSymbolRaw_zero_first_pos])
      (scratch_continuous_localizedSymbolRaw α M m hm i ht))

theorem scratch_periodizedLocalizedFirst_zero_second_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (q : Circle8) (z : ℝ) :
    scratch_periodizedLocalizedFirst α m i t q (-4) z = 0 := by
  unfold scratch_periodizedLocalizedFirst
  apply scratch_liftIcoParam_eq_zero
  intro x
  exact scratch_localizedSymbolRaw_zero_second_neg α m i t x z

theorem scratch_periodizedLocalizedFirst_zero_second_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (q : Circle8) (z : ℝ) :
    scratch_periodizedLocalizedFirst α m i t q 4 z = 0 := by
  unfold scratch_periodizedLocalizedFirst
  apply scratch_liftIcoParam_eq_zero
  intro x
  exact scratch_localizedSymbolRaw_zero_second_pos α m i t x z

theorem scratch_periodizedLocalizedFirst_zero_third_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (q : Circle8) (y : ℝ) :
    scratch_periodizedLocalizedFirst α m i t q y (-4) = 0 := by
  unfold scratch_periodizedLocalizedFirst
  apply scratch_liftIcoParam_eq_zero
  intro x
  exact scratch_localizedSymbolRaw_zero_third_neg α m i t x y

theorem scratch_periodizedLocalizedFirst_zero_third_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) (q : Circle8) (y : ℝ) :
    scratch_periodizedLocalizedFirst α m i t q y 4 = 0 := by
  unfold scratch_periodizedLocalizedFirst
  apply scratch_liftIcoParam_eq_zero
  intro x
  exact scratch_localizedSymbolRaw_zero_third_pos α m i t x y

theorem scratch_periodizedLocalizedFirst_coe
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) {x : ℝ}
    (hx : x ∈ Set.Ico (-4 : ℝ) 4) (y z : ℝ) :
    scratch_periodizedLocalizedFirst α m i t (x : Circle8) y z =
      scratch_localizedSymbolRaw α m i t x y z := by
  unfold scratch_periodizedLocalizedFirst
  exact scratch_liftIcoParam_coe
    (fun x (yz : ℝ × ℝ) ↦ scratch_localizedSymbolRaw α m i t x yz.1 yz.2)
    hx (y, z)

/-- The first two coordinates of the localized symbol, each lifted to a
period-eight additive circle. The circle coordinates are ordered `(y, x)`. -/
noncomputable def scratch_periodizedLocalizedSecond
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) :
    Circle8 → Circle8 → ℝ → ℂ :=
  fun qy qx z ↦ scratch_liftIcoParam
    (fun y (qxz : Circle8 × ℝ) ↦
      scratch_periodizedLocalizedFirst α m i t qxz.1 y qxz.2)
    qy (qx, z)

theorem scratch_continuous_periodizedLocalizedSecond
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (fun p : Circle8 × (Circle8 × ℝ) ↦
      scratch_periodizedLocalizedSecond α m i t p.1 p.2.1 p.2.2) := by
  have hfirst : Continuous (fun p : Circle8 × (ℝ × ℝ) ↦
      scratch_periodizedLocalizedFirst α m i t p.1 p.2.1 p.2.2) :=
    scratch_continuous_periodizedLocalizedFirst α M m hm i ht
  have hreordered : Continuous (fun p : ℝ × (Circle8 × ℝ) ↦
      scratch_periodizedLocalizedFirst α m i t p.2.1 p.1 p.2.2) := by
    exact hfirst.comp
      (continuous_snd.fst.prodMk (continuous_fst.prodMk continuous_snd.snd))
  simpa only [scratch_periodizedLocalizedSecond] using
    (scratch_continuous_liftIcoParam
      (fun y (qxz : Circle8 × ℝ) ↦
        scratch_periodizedLocalizedFirst α m i t qxz.1 y qxz.2)
      (by
        intro qxz
        rw [scratch_periodizedLocalizedFirst_zero_second_neg,
          scratch_periodizedLocalizedFirst_zero_second_pos])
      hreordered)

theorem scratch_periodizedLocalizedSecond_zero_third_neg
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    (qy qx : Circle8) :
    scratch_periodizedLocalizedSecond α m i t qy qx (-4) = 0 := by
  unfold scratch_periodizedLocalizedSecond
  apply scratch_liftIcoParam_eq_zero
  intro y
  exact scratch_periodizedLocalizedFirst_zero_third_neg α m i t qx y

theorem scratch_periodizedLocalizedSecond_zero_third_pos
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    (qy qx : Circle8) :
    scratch_periodizedLocalizedSecond α m i t qy qx 4 = 0 := by
  unfold scratch_periodizedLocalizedSecond
  apply scratch_liftIcoParam_eq_zero
  intro y
  exact scratch_periodizedLocalizedFirst_zero_third_pos α m i t qx y

theorem scratch_periodizedLocalizedSecond_coe
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    {x y : ℝ} (hx : x ∈ Set.Ico (-4 : ℝ) 4)
    (hy : y ∈ Set.Ico (-4 : ℝ) 4) (z : ℝ) :
    scratch_periodizedLocalizedSecond α m i t (y : Circle8) (x : Circle8) z =
      scratch_localizedSymbolRaw α m i t x y z := by
  unfold scratch_periodizedLocalizedSecond
  rw [scratch_liftIcoParam_coe
    (fun y (qxz : Circle8 × ℝ) ↦
      scratch_periodizedLocalizedFirst α m i t qxz.1 y qxz.2) hy]
  exact scratch_periodizedLocalizedFirst_coe α m i t hx y z

/-- All three coordinates of the localized symbol lifted to period-eight
circles, in the order `(z, y, x)`. -/
noncomputable def scratch_periodizedLocalizedThird
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) :
    Circle8 → Circle8 → Circle8 → ℂ :=
  fun qz qy qx ↦ scratch_liftIcoParam
    (fun z (qyqx : Circle8 × Circle8) ↦
      scratch_periodizedLocalizedSecond α m i t qyqx.1 qyqx.2 z)
    qz (qy, qx)

theorem scratch_continuous_periodizedLocalizedThird
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (fun p : Circle8 × (Circle8 × Circle8) ↦
      scratch_periodizedLocalizedThird α m i t p.1 p.2.1 p.2.2) := by
  have hsecond : Continuous (fun p : Circle8 × (Circle8 × ℝ) ↦
      scratch_periodizedLocalizedSecond α m i t p.1 p.2.1 p.2.2) :=
    scratch_continuous_periodizedLocalizedSecond α M m hm i ht
  have hreordered : Continuous (fun p : ℝ × (Circle8 × Circle8) ↦
      scratch_periodizedLocalizedSecond α m i t p.2.1 p.2.2 p.1) := by
    let reorder : ℝ × (Circle8 × Circle8) → Circle8 × (Circle8 × ℝ) :=
      fun p ↦ (p.2.1, (p.2.2, p.1))
    have horder : Continuous reorder := by
      exact (continuous_snd.fst).prodMk
        ((continuous_snd.snd).prodMk continuous_fst)
    change Continuous
      ((fun q : Circle8 × (Circle8 × ℝ) ↦
        scratch_periodizedLocalizedSecond α m i t q.1 q.2.1 q.2.2) ∘ reorder)
    exact hsecond.comp horder
  change Continuous (fun p : Circle8 × (Circle8 × Circle8) ↦
    scratch_liftIcoParam
      (fun z (qyqx : Circle8 × Circle8) ↦
        scratch_periodizedLocalizedSecond α m i t qyqx.1 qyqx.2 z)
      p.1 (p.2.1, p.2.2))
  exact scratch_continuous_liftIcoParam
    (fun z (qyqx : Circle8 × Circle8) ↦
      scratch_periodizedLocalizedSecond α m i t qyqx.1 qyqx.2 z)
    (by
      intro qyqx
      rw [scratch_periodizedLocalizedSecond_zero_third_neg,
        scratch_periodizedLocalizedSecond_zero_third_pos])
    hreordered

theorem scratch_periodizedLocalizedThird_coe
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    {x y z : ℝ} (hx : x ∈ Set.Ico (-4 : ℝ) 4)
    (hy : y ∈ Set.Ico (-4 : ℝ) 4) (hz : z ∈ Set.Ico (-4 : ℝ) 4) :
    scratch_periodizedLocalizedThird α m i t (z : Circle8) (y : Circle8) (x : Circle8) =
      scratch_localizedSymbolRaw α m i t x y z := by
  unfold scratch_periodizedLocalizedThird
  rw [scratch_liftIcoParam_coe
    (fun z (qyqx : Circle8 × Circle8) ↦
      scratch_periodizedLocalizedSecond α m i t qyqx.1 qyqx.2 z) hz]
  exact scratch_periodizedLocalizedSecond_coe α m i t hx hy z

/-- The period-eight extension with the natural `(x, y, z)` order. -/
noncomputable def scratch_periodizedLocalizedSymbol
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) :
    Circle8 × (Circle8 × Circle8) → ℂ :=
  fun p ↦ scratch_periodizedLocalizedThird α m i t p.2.2 p.2.1 p.1

theorem scratch_continuous_periodizedLocalizedSymbol
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (scratch_periodizedLocalizedSymbol α m i t) := by
  have hthird : Continuous (fun p : Circle8 × (Circle8 × Circle8) ↦
      scratch_periodizedLocalizedThird α m i t p.1 p.2.1 p.2.2) :=
    scratch_continuous_periodizedLocalizedThird α M m hm i ht
  let reorder : Circle8 × (Circle8 × Circle8) → Circle8 × (Circle8 × Circle8) :=
    fun p ↦ (p.2.2, (p.2.1, p.1))
  have horder : Continuous reorder := by
    exact (continuous_snd.snd).prodMk
      ((continuous_snd.fst).prodMk continuous_fst)
  change Continuous
    ((fun q : Circle8 × (Circle8 × Circle8) ↦
      scratch_periodizedLocalizedThird α m i t q.1 q.2.1 q.2.2) ∘ reorder)
  exact hthird.comp horder

theorem scratch_periodizedLocalizedSymbol_coe
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    {x y z : ℝ} (hx : x ∈ Set.Ico (-4 : ℝ) 4)
    (hy : y ∈ Set.Ico (-4 : ℝ) 4) (hz : z ∈ Set.Ico (-4 : ℝ) 4) :
    scratch_periodizedLocalizedSymbol α m i t
      ((x : Circle8), ((y : Circle8), (z : Circle8))) =
      scratch_localizedSymbolRaw α m i t x y z := by
  unfold scratch_periodizedLocalizedSymbol
  exact scratch_periodizedLocalizedThird_coe α m i t hx hy hz

/-- Rescale the unit additive circle to the period-eight circle. -/
noncomputable def scratch_unitToCircle8 : UnitAddCircle ≃ₜ Circle8 :=
  (AddCircle.homeomorphAddCircle (8 : ℝ) 1 (by norm_num) (by norm_num)).symm

theorem scratch_unitToCircle8_coe_div (x : ℝ) :
    scratch_unitToCircle8 ((x / 8 : ℝ) : UnitAddCircle) = (x : Circle8) := by
  change (AddCircle.homeomorphAddCircle (8 : ℝ) 1 (by norm_num) (by norm_num)).symm
    ((x / 8 : ℝ) : UnitAddCircle) = (x : Circle8)
  rw [AddCircle.homeomorphAddCircle_symm_apply_mk]
  norm_num

/-- View the period-eight localized symbol as a continuous function on the
standard unit additive three-torus. -/
noncomputable def scratch_unitTorusLocalizedSymbol
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ) :
    UnitAddTorus (Fin 3) → ℂ :=
  fun u ↦ scratch_periodizedLocalizedSymbol α m i t
    (scratch_unitToCircle8 (u 0),
      (scratch_unitToCircle8 (u 1), scratch_unitToCircle8 (u 2)))

theorem scratch_continuous_unitTorusLocalizedSymbol
    (α : Anisotropy) (M : ℝ) (m : E3 → ℂ)
    (hm : Anisotropy.IsAnisotropicMultiplier α M m)
    (i : Fin 3) {t : ℝ} (ht : 0 < t) :
    Continuous (scratch_unitTorusLocalizedSymbol α m i t) := by
  have hperiod : Continuous (scratch_periodizedLocalizedSymbol α m i t) :=
    scratch_continuous_periodizedLocalizedSymbol α M m hm i ht
  have h0 : Continuous (fun u : UnitAddTorus (Fin 3) ↦ scratch_unitToCircle8 (u 0)) :=
    scratch_unitToCircle8.continuous.comp (continuous_apply 0)
  have h1 : Continuous (fun u : UnitAddTorus (Fin 3) ↦ scratch_unitToCircle8 (u 1)) :=
    scratch_unitToCircle8.continuous.comp (continuous_apply 1)
  have h2 : Continuous (fun u : UnitAddTorus (Fin 3) ↦ scratch_unitToCircle8 (u 2)) :=
    scratch_unitToCircle8.continuous.comp (continuous_apply 2)
  let coordinates : UnitAddTorus (Fin 3) → Circle8 × (Circle8 × Circle8) :=
    fun u ↦ (scratch_unitToCircle8 (u 0),
      (scratch_unitToCircle8 (u 1), scratch_unitToCircle8 (u 2)))
  have hcoordinates : Continuous coordinates := h0.prodMk (h1.prodMk h2)
  change Continuous (scratch_periodizedLocalizedSymbol α m i t ∘ coordinates)
  exact hperiod.comp hcoordinates

noncomputable def scratch_unitTorusPoint (x y z : ℝ) : UnitAddTorus (Fin 3) :=
  fun j ↦ ((![x / 8, y / 8, z / 8] j : ℝ) : UnitAddCircle)

theorem scratch_unitTorusLocalizedSymbol_coe_div
    (α : Anisotropy) (m : E3 → ℂ) (i : Fin 3) (t : ℝ)
    {x y z : ℝ} (hx : x ∈ Set.Ico (-4 : ℝ) 4)
    (hy : y ∈ Set.Ico (-4 : ℝ) 4) (hz : z ∈ Set.Ico (-4 : ℝ) 4) :
    scratch_unitTorusLocalizedSymbol α m i t (scratch_unitTorusPoint x y z) =
      scratch_localizedSymbolRaw α m i t x y z := by
  have hx' : scratch_unitToCircle8 (scratch_unitTorusPoint x y z 0) = (x : Circle8) := by
    change scratch_unitToCircle8 ((x / 8 : ℝ) : UnitAddCircle) = (x : Circle8)
    exact scratch_unitToCircle8_coe_div x
  have hy' : scratch_unitToCircle8 (scratch_unitTorusPoint x y z 1) = (y : Circle8) := by
    change scratch_unitToCircle8 ((y / 8 : ℝ) : UnitAddCircle) = (y : Circle8)
    exact scratch_unitToCircle8_coe_div y
  have hz' : scratch_unitToCircle8 (scratch_unitTorusPoint x y z 2) = (z : Circle8) := by
    change scratch_unitToCircle8 ((z / 8 : ℝ) : UnitAddCircle) = (z : Circle8)
    exact scratch_unitToCircle8_coe_div z
  change scratch_periodizedLocalizedSymbol α m i t
    (scratch_unitToCircle8 (scratch_unitTorusPoint x y z 0),
      (scratch_unitToCircle8 (scratch_unitTorusPoint x y z 1),
        scratch_unitToCircle8 (scratch_unitTorusPoint x y z 2))) = _
  rw [hx', hy', hz']
  exact scratch_periodizedLocalizedSymbol_coe α m i t hx hy hz

end
end Auto.Twisted
