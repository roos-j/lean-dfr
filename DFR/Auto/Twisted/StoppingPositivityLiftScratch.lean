/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto

open MeasureTheory
open scoped BigOperators ENNReal NNReal FourierTransform RealInnerProductSpace

noncomputable section

namespace Twisted

/-- A positive one-box size forces the finite ancestor stopping datum to be
positive, since the box itself is one of its ancestors. -/
theorem scratch_stoppingData_pos_of_boxLocalSize_pos
    (α : Anisotropy) (S : BoxCollection α) (s : ℝ) (f : E3 → ℝ)
    {q : AnisoBox α} (hq : q ∈ S)
    (hsize : 0 < boxLocalSize α s f q) :
    0 < stoppingData α S s f q :=
  lt_of_lt_of_le hsize (boxLocalSize_le_stoppingData α S s f hq)

/-- If the second and fourth one-box local sizes are positive, then the
literal source stopping data are positive in all four coordinates. -/
theorem scratch_sourceStoppingData_pos_of_two_four_sizes
    (α : Anisotropy) (S : BoxCollection α) (f : ModelRealInput)
    (htwo : ∀ q ∈ S, 0 < boxLocalSize α 2 (f 3) q)
    (hfour : ∀ q ∈ S, ∀ j : Fin 4, j ≠ 3 →
      0 < boxLocalSize α 4 (f j) q) :
    ∀ q ∈ S, ∀ j,
      0 < stoppingData α S (sourceStoppingExponent j) (f j) q := by
  intro q hq j
  by_cases hj : j = 3
  · subst j
    rw [sourceStoppingExponent_eq_two_of_eq_three rfl]
    exact scratch_stoppingData_pos_of_boxLocalSize_pos
      α S 2 (f 3) hq (htwo q hq)
  · rw [sourceStoppingExponent_eq_four_of_ne_three hj]
    exact scratch_stoppingData_pos_of_boxLocalSize_pos
      α S 4 (f j) hq (hfour q hq j hj)

/-- The already established fourth-size positivity supplies every non-active
coordinate of the source stopping datum for bounded continuous nonzero data. -/
theorem scratch_sourceStoppingData_pos_four_coordinates
    (α : Anisotropy) (S : BoxCollection α) (f : ModelRealInput)
    (hcont : ∀ j, Continuous (f j))
    (hbound : ∀ j, ∃ B : ℝ, 0 ≤ B ∧ ∀ y, |f j y| ≤ B)
    (hnonzero : ∀ j, ∃ y, f j y ≠ 0) :
    ∀ q ∈ S, ∀ j : Fin 4, j ≠ 3 →
      0 < stoppingData α S 4 (f j) q := by
  intro q hq j hj
  rcases hnonzero j with ⟨y, hy⟩
  exact scratch_stoppingData_pos_of_boxLocalSize_pos α S 4 (f j) hq
    (boxLocalSize_four_pos_of_continuous_nonzero
      α q (f j) (hcont j) (hbound j) hy)

/-- For a real Schwartz tuple with nonzero components, a positive second
local-size input at the active coordinate completes all source stopping-data
positivity conditions. -/
theorem scratch_initialModel_sourceStoppingData_pos_of_two_size
    (α : Anisotropy) (S : BoxCollection α) (F : InitialModelSchwartzInput)
    (hFnonzero : ∀ j, F j ≠ 0)
    (htwo : ∀ q ∈ S,
      0 < boxLocalSize α 2 (InitialModelRealInput F 3) q) :
    ∀ q ∈ S, ∀ j,
      0 < stoppingData α S (sourceStoppingExponent j)
        (InitialModelRealInput F j) q := by
  apply scratch_sourceStoppingData_pos_of_two_four_sizes
    α S (InitialModelRealInput F) htwo
  intro q hq j hj
  have hpoint : ∃ y, InitialModelRealInput F j y ≠ 0 := by
    by_contra h
    have hzero : ∀ y, InitialModelRealInput F j y = 0 := by
      intro y
      by_contra hy
      exact h ⟨y, hy⟩
    apply hFnonzero j
    ext y
    exact hzero y
  rcases hpoint with ⟨y, hy⟩
  exact boxLocalSize_four_pos_of_continuous_nonzero
    α q (InitialModelRealInput F j)
    (initialModelRealInput_continuous F j)
    (initialModelRealInput_bounded F j) hy

end Twisted

end

end Auto
