/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory
open scoped ENNReal

noncomputable section

/-! ## Finite-scale good-part `L²` estimates

The source's preliminary finite-scale Young--Hölder estimate has a useful
output-side specialization: at one positive scale, the three convolved input
slots in `L⁶` give an `L²` spatial product.  This is independent of the
strict initial-range form estimate and is the appropriate good-part input for
a finite-interval Calderón--Zygmund argument.
-/

/-- At one positive scale, the literal three-factor model-operator integrand
maps three measurable `L⁶` inputs to `L²`, with precisely the three
one-dimensional Young constants. -/
theorem scratch_eLpNorm_modelTruncatedOperatorIntegrand_le_six
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (f : ModelOperatorRealInput)
    (t : ℝ) (ht : 0 < t) (hf : ∀ j, Measurable (f j)) :
    eLpNorm (modelTruncatedOperatorIntegrand α u c f t) 2 volume ≤
      ENNReal.ofReal |c t| *
        (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
          eLpNorm (f 0) 6 volume) *
        (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
          eLpNorm (f 1) 6 volume) *
        (ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
          eLpNorm (f 2) 6 volume) := by
  let A : E3 → ℝ := ModelCoordinateConvolution 0 (f 0)
    (ModelLowKernel (u 0)) (t ^ α.weight 0)
  let B : E3 → ℝ := ModelCoordinateConvolution 1 (f 1)
    (ModelLowKernel (u 1)) (t ^ α.weight 1)
  let D : E3 → ℝ := ModelCoordinateConvolution 2 (f 2)
    (ModelThirdKernel (u 2)) (t ^ α.weight 2)
  have hs0 : 0 < t ^ α.weight 0 := pow_pos ht _
  have hs1 : 0 < t ^ α.weight 1 := pow_pos ht _
  have hs2 : 0 < t ^ α.weight 2 := pow_pos ht _
  have hAmeas : AEStronglyMeasurable A volume :=
    (stronglyMeasurable_ModelCoordinateConvolution_of_measurable
      0 (f 0) (ModelLowKernel (u 0)) (t ^ α.weight 0)
      (hf 0) (measurable_ModelLowKernel (u 0))).aestronglyMeasurable
  have hBmeas : AEStronglyMeasurable B volume :=
    (stronglyMeasurable_ModelCoordinateConvolution_of_measurable
      1 (f 1) (ModelLowKernel (u 1)) (t ^ α.weight 1)
      (hf 1) (measurable_ModelLowKernel (u 1))).aestronglyMeasurable
  have hDmeas : AEStronglyMeasurable D volume :=
    (stronglyMeasurable_ModelCoordinateConvolution_of_measurable
      2 (f 2) (ModelThirdKernel (u 2)) (t ^ α.weight 2)
      (hf 2) (measurable_ModelThirdKernel (u 2))).aestronglyMeasurable
  have hA := eLpNorm_ModelCoordinateConvolution_le
    0 6 (by norm_num) (f 0) (ModelLowKernel (u 0)) (hf 0)
    (measurable_ModelLowKernel (u 0)) (integrable_ModelLowKernel (u 0)) hs0
  have hB := eLpNorm_ModelCoordinateConvolution_le
    1 6 (by norm_num) (f 1) (ModelLowKernel (u 1)) (hf 1)
    (measurable_ModelLowKernel (u 1)) (integrable_ModelLowKernel (u 1)) hs1
  have hD := eLpNorm_ModelCoordinateConvolution_le
    2 6 (by norm_num) (f 2) (ModelThirdKernel (u 2)) (hf 2)
    (measurable_ModelThirdKernel (u 2)) (integrable_ModelThirdKernel (u 2)) hs2
  letI : ENNReal.HolderTriple (6 : ℝ≥0∞) 6 3 :=
    ENNReal.HolderTriple.of_toReal (by constructor <;> norm_num)
  have hAB : eLpNorm (fun x ↦ A x * B x) 3 volume ≤
      eLpNorm A 6 volume * eLpNorm B 6 volume := by
    change eLpNorm (A * B) 3 volume ≤ _
    simpa only [smul_eq_mul] using
      (eLpNorm_smul_le_mul_eLpNorm hBmeas hAmeas)
  letI : ENNReal.HolderTriple (6 : ℝ≥0∞) 3 2 :=
    ENNReal.HolderTriple.of_toReal (by constructor <;> norm_num)
  have hABD : eLpNorm (fun x ↦ A x * B x * D x) 2 volume ≤
      eLpNorm A 6 volume * eLpNorm B 6 volume * eLpNorm D 6 volume := by
    have h := eLpNorm_smul_le_mul_eLpNorm
      (p := 6) (q := 3) (r := 2)
      (f := fun x ↦ A x * B x) (φ := D) (μ := volume)
      (hAmeas.mul hBmeas) hDmeas
    calc
      eLpNorm (fun x ↦ A x * B x * D x) 2 volume =
          eLpNorm (fun x ↦ D x * (A x * B x)) 2 volume := by
            apply eLpNorm_congr_ae
            filter_upwards [] with x
            ring
      _ ≤ eLpNorm D 6 volume * eLpNorm (fun x ↦ A x * B x) 3 volume := by
            change eLpNorm (D * fun x ↦ A x * B x) 2 volume ≤ _
            exact h
      _ ≤ eLpNorm D 6 volume *
          (eLpNorm A 6 volume * eLpNorm B 6 volume) := by gcongr
      _ = eLpNorm A 6 volume * eLpNorm B 6 volume * eLpNorm D 6 volume := by
            ring
  change eLpNorm (fun x ↦ c t * A x * B x * D x) 2 volume ≤ _
  calc
    eLpNorm (fun x ↦ c t * A x * B x * D x) 2 volume =
        eLpNorm (c t • fun x ↦ A x * B x * D x) 2 volume := by
          congr 1
          funext x
          change c t * A x * B x * D x = c t * (A x * B x * D x)
          ring
    _ = ‖c t‖ₑ * eLpNorm (fun x ↦ A x * B x * D x) 2 volume :=
      eLpNorm_const_smul _ _ _ _
    _ ≤ ‖c t‖ₑ *
        (eLpNorm A 6 volume * eLpNorm B 6 volume * eLpNorm D 6 volume) := by
          gcongr
    _ ≤ ENNReal.ofReal |c t| *
        (eLpNorm A 6 volume * eLpNorm B 6 volume * eLpNorm D 6 volume) := by
          have hcoeff : ‖c t‖ₑ ≤ ENNReal.ofReal |c t| := by
            exact le_of_eq (Real.enorm_eq_ofReal_abs _)
          exact mul_le_mul hcoeff le_rfl (by positivity) (by positivity)
    _ ≤ ENNReal.ofReal |c t| *
        (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
          eLpNorm (f 0) 6 volume) *
        (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
          eLpNorm (f 1) 6 volume) *
        (ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
          eLpNorm (f 2) 6 volume) := by
          have hA' : eLpNorm A 6 volume ≤
              ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
                eLpNorm (f 0) 6 volume := by
            simpa only [A, ENNReal.ofReal_ofNat] using hA
          have hB' : eLpNorm B 6 volume ≤
              ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
                eLpNorm (f 1) 6 volume := by
            simpa only [B, ENNReal.ofReal_ofNat] using hB
          have hD' : eLpNorm D 6 volume ≤
              ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
                eLpNorm (f 2) 6 volume := by
            simpa only [D, ENNReal.ofReal_ofNat] using hD
          have hAB' : eLpNorm A 6 volume * eLpNorm B 6 volume ≤
              (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
                eLpNorm (f 0) 6 volume) *
              (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
                eLpNorm (f 1) 6 volume) := by
            exact mul_le_mul hA' hB' (by positivity) (by positivity)
          have hABD' : eLpNorm A 6 volume * eLpNorm B 6 volume *
              eLpNorm D 6 volume ≤
              ((ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
                eLpNorm (f 0) 6 volume) *
                (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
                  eLpNorm (f 1) 6 volume)) *
                (ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
                  eLpNorm (f 2) 6 volume) := by
            exact mul_le_mul hAB' hD' (by positivity) (by positivity)
          calc
            ENNReal.ofReal |c t| *
                (eLpNorm A 6 volume * eLpNorm B 6 volume * eLpNorm D 6 volume) ≤
                ENNReal.ofReal |c t| *
                  (((ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
                    eLpNorm (f 0) 6 volume) *
                    (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
                      eLpNorm (f 1) 6 volume)) *
                    (ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
                      eLpNorm (f 2) 6 volume)) :=
              mul_le_mul_of_nonneg_left hABD' (by positivity)
            _ = ENNReal.ofReal |c t| *
                (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 0) y|) *
                  eLpNorm (f 0) 6 volume) *
                (ENNReal.ofReal (∫ y : ℝ, |ModelLowKernel (u 1) y|) *
                  eLpNorm (f 1) 6 volume) *
                (ENNReal.ofReal (∫ y : ℝ, |ModelThirdKernel (u 2) y|) *
                  eLpNorm (f 2) 6 volume) := by ring

end
end Twisted
end Auto
