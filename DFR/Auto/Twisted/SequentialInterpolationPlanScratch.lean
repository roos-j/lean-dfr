/- Copyright (c) 2026 Joris Roos. All rights reserved.
License: Apache-2.0
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

/-! ### Sequential interpolation exponents

The source presents the extended range through a four-vertex multilinear
Marcinkiewicz theorem.  For the literal strong-to-weak fiber extension proved
in Section 8, the same range can instead be reached by three unary
interpolations: at each stage choose the weak endpoint strictly beyond the
desired reciprocal exponent, interpolate back to the desired exponent, and
then use the resulting strong estimate as input to the next fiber extension.
This file records the exact arithmetic slack needed for that route.
-/

namespace Auto.Twisted

noncomputable section

/-- Endpoint data for the three sequential unary interpolation steps.  Each
endpoint remains strictly below output reciprocal one, so the next strong to
weak fiber extension is available. -/
structure scratch_SequentialInterpolationPlan
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3) where
  γ1 : ℝ
  γ2 : ℝ
  γ3 : ℝ
  beta1_lt_gamma1 : β1 < γ1
  gamma1_output_lt_one : γ1 + b.b2 + b.b3 < 1
  beta2_lt_gamma2 : β2 < γ2
  gamma2_output_lt_one : β1 + γ2 + b.b3 < 1
  beta3_lt_gamma3 : β3 < γ3
  gamma3_output_lt_one : β1 + β2 + γ3 < 1

/-- Strict target-to-endpoint interpolation weights for the three stages. -/
noncomputable def scratch_sequentialInterpolationWeight
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3)
    (P : scratch_SequentialInterpolationPlan b) (j : Fin 3) : ℝ :=
  match j with
  | 0 => (β1 - b.b1) / (P.γ1 - b.b1)
  | 1 => (β2 - b.b2) / (P.γ2 - b.b2)
  | 2 => (β3 - b.b3) / (P.γ3 - b.b3)

/-- The sequential endpoints exist with strict room on every stage. -/
theorem scratch_exists_sequentialInterpolationPlan
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3) :
    Nonempty (scratch_SequentialInterpolationPlan b) := by
  let γ1 : ℝ := (β1 + (1 - b.b2 - b.b3)) / 2
  let γ2 : ℝ := (β2 + (1 - β1 - b.b3)) / 2
  let γ3 : ℝ := (β3 + (1 - β1 - β2)) / 2
  have hγ1room : β1 < 1 - b.b2 - b.b3 := by
    linarith [b.b2_lt, b.b3_lt, b.beta_sum_eq_one, b.beta0_pos]
  have hγ2room : β2 < 1 - β1 - b.b3 := by
    linarith [b.b3_lt, b.beta_sum_eq_one, b.beta0_pos]
  have hγ3room : β3 < 1 - β1 - β2 := by
    linarith [b.beta_sum_eq_one, b.beta0_pos]
  refine ⟨⟨γ1, γ2, γ3, ?_, ?_, ?_, ?_, ?_, ?_⟩⟩ <;>
    dsimp only [γ1, γ2, γ3] <;> linarith

/-- Every sequential interpolation weight lies strictly between zero and
one.  Thus each target exponent is genuinely interior to its unary endpoint
segment. -/
theorem scratch_sequentialInterpolationWeight_mem_Ioo
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3)
    (P : scratch_SequentialInterpolationPlan b) (j : Fin 3) :
    scratch_sequentialInterpolationWeight b P j ∈ Set.Ioo (0 : ℝ) 1 := by
  fin_cases j
  · have hden : 0 < P.γ1 - b.b1 := by
      linarith [P.beta1_lt_gamma1, b.b1_lt]
    constructor
    · exact div_pos (sub_pos.mpr b.b1_lt) hden
    · apply (div_lt_one hden).mpr
      linarith [P.beta1_lt_gamma1]
  · have hden : 0 < P.γ2 - b.b2 := by
      linarith [P.beta2_lt_gamma2, b.b2_lt]
    constructor
    · exact div_pos (sub_pos.mpr b.b2_lt) hden
    · apply (div_lt_one hden).mpr
      linarith [P.beta2_lt_gamma2]
  · have hden : 0 < P.γ3 - b.b3 := by
      linarith [P.beta3_lt_gamma3, b.b3_lt]
    constructor
    · exact div_pos (sub_pos.mpr b.b3_lt) hden
    · apply (div_lt_one hden).mpr
      linarith [P.beta3_lt_gamma3]

/-- The first target reciprocal exponent is the convex interpolation point
between the base and the first chosen weak endpoint. -/
theorem scratch_sequentialInterpolationWeight_first_identity
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3)
    (P : scratch_SequentialInterpolationPlan b) :
    β1 = (1 - scratch_sequentialInterpolationWeight b P 0) * b.b1 +
      scratch_sequentialInterpolationWeight b P 0 * P.γ1 := by
  have hden : P.γ1 - b.b1 ≠ 0 := by
    linarith [P.beta1_lt_gamma1, b.b1_lt]
  dsimp [scratch_sequentialInterpolationWeight]
  field_simp
  ring

/-- The analogous second-coordinate interpolation identity. -/
theorem scratch_sequentialInterpolationWeight_second_identity
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3)
    (P : scratch_SequentialInterpolationPlan b) :
    β2 = (1 - scratch_sequentialInterpolationWeight b P 1) * b.b2 +
      scratch_sequentialInterpolationWeight b P 1 * P.γ2 := by
  have hden : P.γ2 - b.b2 ≠ 0 := by
    linarith [P.beta2_lt_gamma2, b.b2_lt]
  dsimp [scratch_sequentialInterpolationWeight]
  field_simp
  ring

/-- The analogous third-coordinate interpolation identity. -/
theorem scratch_sequentialInterpolationWeight_third_identity
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3)
    (P : scratch_SequentialInterpolationPlan b) :
    β3 = (1 - scratch_sequentialInterpolationWeight b P 2) * b.b3 +
      scratch_sequentialInterpolationWeight b P 2 * P.γ3 := by
  have hden : P.γ3 - b.b3 ≠ 0 := by
    linarith [P.beta3_lt_gamma3, b.b3_lt]
  dsimp [scratch_sequentialInterpolationWeight]
  field_simp
  ring

end

end Auto.Twisted
