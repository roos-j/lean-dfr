/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.TrilinearOpenNeighborhoodScratch

namespace Auto
namespace Twisted

noncomputable section

/-! ## The open reciprocal-exponent neighbourhood of the source simplex

The strict barycentric inequalities in `ExponentSimplex` do more than put
the target at the convex hull of the four weak vertices: they put it in its
interior.  The finite dyadic summation needs precisely the eight signwise
perturbations below.  This file makes their common radius explicit.
-/

/-- Perturb a reciprocal exponent to the side selected by one dyadic
amplitude sign. -/
def scratch_signedReciprocalPerturbation (positive : Bool) (x ε : ℝ) : ℝ :=
  if positive = true then x + ε else x - ε

/-- Every signwise perturbation of the target reciprocal vector by a common
small radius remains strictly inside the tetrahedron with base `b` and edge
vertices `b + b₀ e_j`.  In particular, it has positive input and output
reciprocals and is on the strict summable side of each target coordinate. -/
theorem scratch_exponentSimplex_uniform_signed_open_neighborhood
    {β0 β1 β2 β3 : ℝ} (b : ExponentSimplex β0 β1 β2 β3) :
    ∃ ε : ℝ, 0 < ε ∧ ∀ (positive₁ positive₂ positive₃ : Bool),
      let a₁ := scratch_signedReciprocalPerturbation positive₁ β1 ε
      let a₂ := scratch_signedReciprocalPerturbation positive₂ β2 ε
      let a₃ := scratch_signedReciprocalPerturbation positive₃ β3 ε
      b.b1 < a₁ ∧ b.b2 < a₂ ∧ b.b3 < a₃ ∧
      0 < a₁ + a₂ + a₃ ∧ a₁ + a₂ + a₃ < 1 ∧
      (if positive₁ = true then β1 < a₁ else a₁ < β1) ∧
      (if positive₂ = true then β2 < a₂ else a₂ < β2) ∧
      (if positive₃ = true then β3 < a₃ else a₃ < β3) := by
  let δ : ℝ := min (β1 - b.b1)
    (min (β2 - b.b2) (min (β3 - b.b3) (β0 / 3)))
  let ε : ℝ := δ / 2
  have hgap₁ : 0 < β1 - b.b1 := sub_pos.mpr b.b1_lt
  have hgap₂ : 0 < β2 - b.b2 := sub_pos.mpr b.b2_lt
  have hgap₃ : 0 < β3 - b.b3 := sub_pos.mpr b.b3_lt
  have hβ0third : 0 < β0 / 3 := by linarith [b.beta0_pos]
  have hδpos : 0 < δ := by
    dsimp [δ]
    exact lt_min hgap₁ (lt_min hgap₂ (lt_min hgap₃ hβ0third))
  have hεpos : 0 < ε := by
    dsimp [ε]
    linarith
  have hδgap₁ : δ ≤ β1 - b.b1 := by
    dsimp [δ]
    exact min_le_left _ _
  have hδgap₂ : δ ≤ β2 - b.b2 := by
    dsimp [δ]
    exact le_trans (min_le_right _ _) (min_le_left _ _)
  have hδgap₃ : δ ≤ β3 - b.b3 := by
    dsimp [δ]
    exact le_trans (min_le_right _ _)
      (le_trans (min_le_right _ _) (min_le_left _ _))
  have hδβ0 : δ ≤ β0 / 3 := by
    dsimp [δ]
    exact le_trans (min_le_right _ _)
      (le_trans (min_le_right _ _) (min_le_right _ _))
  have hεgap₁ : ε < β1 - b.b1 := by
    dsimp [ε]
    linarith
  have hεgap₂ : ε < β2 - b.b2 := by
    dsimp [ε]
    linarith
  have hεgap₃ : ε < β3 - b.b3 := by
    dsimp [ε]
    linarith
  have hthreeε : 3 * ε < β0 := by
    dsimp [ε]
    linarith
  refine ⟨ε, hεpos, ?_⟩
  intro positive₁ positive₂ positive₃
  have hlower₁ : b.b1 < scratch_signedReciprocalPerturbation positive₁ β1 ε := by
    by_cases hpositive : positive₁ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hlower₂ : b.b2 < scratch_signedReciprocalPerturbation positive₂ β2 ε := by
    by_cases hpositive : positive₂ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hlower₃ : b.b3 < scratch_signedReciprocalPerturbation positive₃ β3 ε := by
    by_cases hpositive : positive₃ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hside₁ : if positive₁ = true then
      β1 < scratch_signedReciprocalPerturbation positive₁ β1 ε else
        scratch_signedReciprocalPerturbation positive₁ β1 ε < β1 := by
    by_cases hpositive : positive₁ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hside₂ : if positive₂ = true then
      β2 < scratch_signedReciprocalPerturbation positive₂ β2 ε else
        scratch_signedReciprocalPerturbation positive₂ β2 ε < β2 := by
    by_cases hpositive : positive₂ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hside₃ : if positive₃ = true then
      β3 < scratch_signedReciprocalPerturbation positive₃ β3 ε else
        scratch_signedReciprocalPerturbation positive₃ β3 ε < β3 := by
    by_cases hpositive : positive₃ = true
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
    · simp [scratch_signedReciprocalPerturbation, hpositive]
      linarith
  have hsumlt : scratch_signedReciprocalPerturbation positive₁ β1 ε +
      scratch_signedReciprocalPerturbation positive₂ β2 ε +
      scratch_signedReciprocalPerturbation positive₃ β3 ε < 1 := by
    by_cases h₁ : positive₁ = true <;>
      by_cases h₂ : positive₂ = true <;>
      by_cases h₃ : positive₃ = true <;>
      simp [scratch_signedReciprocalPerturbation, h₁, h₂, h₃] <;>
      linarith [b.beta_sum_eq_one]
  refine ⟨hlower₁, hlower₂, hlower₃, ?_, hsumlt, hside₁, hside₂, hside₃⟩
  linarith [b.b1_pos, b.b2_pos, b.b3_pos]

end
end Twisted
end Auto
