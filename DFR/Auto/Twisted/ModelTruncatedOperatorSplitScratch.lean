/- Copyright (c) 2026 Joris Roos. All rights reserved.
Released under Apache 2.0 license as described in the file LICENSE.
This file was automatically generated by Codex.
-/

import Auto.Twisted.Twisted

namespace Auto
namespace Twisted

open MeasureTheory Filter

noncomputable section

/-! ## One-input finite-scale splitting of the truncated model operator -/

/-- A coordinate model convolution is additive once the two literal fiber
integrands are integrable. -/
theorem scratch_ModelCoordinateConvolution_add
    (i : Fin 3) (f g : E3 → ℝ) (k : ℝ → ℝ) (s : ℝ) (x : E3)
    (hf : Integrable (fun r : ℝ ↦
      f (x - r • Anisotropy.coordinateDirection i) * kernelDilate k s r))
    (hg : Integrable (fun r : ℝ ↦
      g (x - r • Anisotropy.coordinateDirection i) * kernelDilate k s r)) :
    ModelCoordinateConvolution i (f + g) k s x =
      ModelCoordinateConvolution i f k s x +
        ModelCoordinateConvolution i g k s x := by
  unfold ModelCoordinateConvolution
  rw [show (fun r : ℝ ↦
      (f + g) (x - r • Anisotropy.coordinateDirection i) * kernelDilate k s r) =
      (fun r ↦ f (x - r • Anisotropy.coordinateDirection i) *
          kernelDilate k s r) +
        (fun r ↦ g (x - r • Anisotropy.coordinateDirection i) *
          kernelDilate k s r) by
      funext r
      simp only [Pi.add_apply]
      ring]
  change (∫ r : ℝ,
      f (x - r • Anisotropy.coordinateDirection i) * kernelDilate k s r +
        g (x - r • Anisotropy.coordinateDirection i) * kernelDilate k s r) =
      (∫ r : ℝ, f (x - r • Anisotropy.coordinateDirection i) *
        kernelDilate k s r) +
        ∫ r : ℝ, g (x - r • Anisotropy.coordinateDirection i) *
          kernelDilate k s r
  exact integral_add hf hg

/-- The fixed-scale model-operator integrand is additive in its first input
slot once the two literal line-convolution integrands are integrable.  The
other two slots are kept fixed in the common background input `f`. -/
theorem scratch_modelTruncatedOperatorIntegrand_slot0_add
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (f : ModelOperatorRealInput)
    (g h : E3 → ℝ) (t : ℝ) (x : E3)
    (hg : Integrable (fun r : ℝ ↦
      g (x - r • Anisotropy.coordinateDirection 0) *
        kernelDilate (ModelLowKernel (u 0)) (t ^ α.weight 0) r))
    (hh : Integrable (fun r : ℝ ↦
      h (x - r • Anisotropy.coordinateDirection 0) *
        kernelDilate (ModelLowKernel (u 0)) (t ^ α.weight 0) r)) :
    modelTruncatedOperatorIntegrand α u c (Function.update f 0 (g + h)) t x =
      modelTruncatedOperatorIntegrand α u c (Function.update f 0 g) t x +
        modelTruncatedOperatorIntegrand α u c (Function.update f 0 h) t x := by
  unfold modelTruncatedOperatorIntegrand
  simp
  rw [scratch_ModelCoordinateConvolution_add 0 g h (ModelLowKernel (u 0))
    (t ^ α.weight 0) x hg hh]
  ring

/-- The finite-scale truncated operator is additive in its first input slot.
The line-integrability assumptions justify the fixed-scale identity at every
scale, while the two final hypotheses are the explicit scale-integrability
premises needed by `integral_add` on the truncated interval. -/
theorem scratch_ModelTruncatedOperator_slot0_add
    (α : Anisotropy) (u : E3) (c : ℝ → ℝ) (f : ModelOperatorRealInput)
    (g h : E3 → ℝ) (a b : ℝ) (x : E3)
    (hlineg : ∀ t : ℝ, Integrable (fun r : ℝ ↦
      g (x - r • Anisotropy.coordinateDirection 0) *
        kernelDilate (ModelLowKernel (u 0)) (t ^ α.weight 0) r))
    (hlineh : ∀ t : ℝ, Integrable (fun r : ℝ ↦
      h (x - r • Anisotropy.coordinateDirection 0) *
        kernelDilate (ModelLowKernel (u 0)) (t ^ α.weight 0) r))
    (hscaleg : Integrable (fun t : ℝ ↦
      modelTruncatedOperatorIntegrand α u c (Function.update f 0 g) t x)
      (((volume : Measure ℝ).withDensity cubeScaleDensity).restrict (Set.Ioc a b)))
    (hscaleh : Integrable (fun t : ℝ ↦
      modelTruncatedOperatorIntegrand α u c (Function.update f 0 h) t x)
      (((volume : Measure ℝ).withDensity cubeScaleDensity).restrict (Set.Ioc a b))) :
    ModelTruncatedOperator α u c (Function.update f 0 (g + h)) a b x =
      ModelTruncatedOperator α u c (Function.update f 0 g) a b x +
        ModelTruncatedOperator α u c (Function.update f 0 h) a b x := by
  have hfun : (fun t : ℝ ↦
      modelTruncatedOperatorIntegrand α u c (Function.update f 0 (g + h)) t x) =
      (fun t ↦ modelTruncatedOperatorIntegrand α u c (Function.update f 0 g) t x +
        modelTruncatedOperatorIntegrand α u c (Function.update f 0 h) t x) := by
    funext t
    exact scratch_modelTruncatedOperatorIntegrand_slot0_add α u c f g h t x
      (hlineg t) (hlineh t)
  unfold ModelTruncatedOperator
  rw [hfun]
  exact integral_add hscaleg hscaleh

end
end Twisted
end Auto
