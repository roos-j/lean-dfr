import DFR.Defs

/-!

# Main theorems

Here we state the main theorems of the paper.
The proofs are in the `Auto` folder.

-/

theorem my_theorem (a b : ℕ) (hab : a < b) :
    a ≠ b := by
  sorry

#print axioms my_theorem
